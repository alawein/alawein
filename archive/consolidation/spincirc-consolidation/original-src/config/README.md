# Config

This directory is reserved for configuration files (for example tool
settings or environment templates) for **SpinCirc**.

It currently exists to satisfy the workspace golden-template
`config/` best-practice requirement and to provide a clear home for
future configuration.

