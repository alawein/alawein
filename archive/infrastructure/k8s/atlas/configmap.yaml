# ATLAS Multi-Agent Orchestration Platform
# Kubernetes ConfigMap

apiVersion: v1
kind: ConfigMap
metadata:
  name: atlas-config
  labels:
    app: atlas
data:
  NODE_ENV: 'production'
  ATLAS_PORT: '3000'
  ATLAS_STORAGE_BACKEND: 'sqlite'
  ATLAS_LOG_LEVEL: 'info'
