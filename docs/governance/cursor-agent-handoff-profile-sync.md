---
title: Cursor agent handoff — profile sync and PKOS alignment
description: Summary, revision plan, and syncing runbook for PKOS and alawein profile/README alignment
category: governance
audience: [ai-agents, contributors]
status: active
last_updated: 2026-03-20
---

# Cursor agent handoff — profile sync and PKOS alignment

**Use this doc when:** Continuing work on profile/positioning sync, README canonicalization, `_pkos` rename/location, or meshal-web/LinkedIn export. Treat it as the single handoff for revision and enhancements.

---

## 1. Summary of current state

### What’s implemented

| Area | Location | What |
|------|----------|------|
| **Canonical README** | alawein | Single `README.md` (no backup file). `scripts/sync-readme.py` rewrites SYNC:PROJECTS, SYNC:RESEARCH, SYNC:PACKAGES, and SYNC:PROFILE from `projects.json` and optional `profile-from-guides.yaml`. |
| **Profile export (PKOS)** | _pkos (formerly guides) | `schema/export/profile-export.schema.yaml`, `templates/alawein-readme.yaml`, `templates/meshal-web.yaml`, `templates/linkedin.yaml`, `docs/career/profile-copy.yaml`, `scripts/export-profile.py`. Export writes `out/profile-export.yaml` and can write `profile-from-guides.yaml` into alawein. |
| **Profile sync into README** | alawein | README.md has `<!-- SYNC:PROFILE:START -->` … `<!-- SYNC:PROFILE:END -->`. When `profile-from-guides.yaml` exists and PyYAML is installed, `sync-readme.py` regenerates that block from the YAML. File is gitignored. |
| **Governance docs** | alawein | `docs/governance/profile-sync-from-guides.md` (flow, quality gates, `_pkos` naming). AGENTS.md, CLAUDE.md reference it. |
| **_pkos naming** | both | Docs say canonical name is `_pkos`; rename from `guides` when folder not in use. Physical folder may still be `Desktop/guides` until user runs rename. |

### Key paths

| Repo / dir | Path | Purpose |
|------------|------|---------|
| **alawein** | `README.md` | Canonical org README; all SYNC blocks live here. |
| **alawein** | `projects.json` | Source for projects/research/packages sync. |
| **alawein** | `profile-from-guides.yaml` | Generated by `_pkos`; gitignored. |
| **alawein** | `scripts/sync-readme.py` | Single script for projects + profile sync. |
| **_pkos** | `C:\Users\mesha\Desktop\guides` or `...\Desktop\_pkos` | PKOS root. Profile: `db/profile/meshal-alawein.md`. Copy: `docs/career/profile-copy.yaml`. Export: `scripts/export-profile.py`. |
| **_pkos** | `schema/export/profile-export.schema.yaml` | Canonical export schema. |
| **_pkos** | `templates/*.yaml` | Platform field maps (alawein-readme, meshal-web, linkedin, notion, cv-master). |

### Not yet done (pending)

- **Rename folder:** `Desktop/guides` → `Desktop/_pkos` (user must run when folder not in use).
- **meshal-web:** Consume `profile-export.yaml` or a generated artifact in build; no automation yet. Manual option: copy `out/profile-export.yaml` (or relevant fields) into meshal-web and document in that repo’s README that profile content comes from `_pkos` export.
- **_pkos in workspace:** Optional move to sibling of alawein (e.g. `../GitHub/github.com/_pkos`); then set `ALAWEIN_REPO_PATH` or update default in `export-profile.py` and HANDOFF-CODEX paths.

---

## 2. Revision and enhancements plan

Execute in order when improving or extending the setup.

### Phase A — Consistency and hygiene

1. **Replace any remaining “guides” wording** in alawein with “_pkos (formerly guides)” or “_pkos” where it refers to the PKOS repo. Check: `profile-sync-from-guides.md`, Files table (“Generated by guides” → “Generated by _pkos”), Quality gates (“guides or the exported YAML” → “_pkos or the exported YAML”).
2. **_pkos templates/README.md** — Update “guides root” to “_pkos root” and “alawein/alawein” references so they stay correct after rename.
3. **Alawein doc contract** — If `scripts/validate-doc-contract.sh` or `docs/governance/documentation-contract.md` still mention `README-backup-20250807.md` or `docs/README-backup-20250807.md`, update to `README.md` and `docs/README.md` only.
4. **Deprecate `docs/README-backup-20250807.md`** — Keep as a short stub that points to `../README.md` and `docs/README.md`; or remove and add a redirect in docs index if the repo supports it.

### Phase B — Export and sync robustness

- **Export script default path** — In `_pkos` `scripts/export-profile.py`, make the default alawein path configurable (e.g. env `ALAWEIN_REPO_PATH` or a small config file) so it works when `_pkos` is at `Desktop/_pkos` or when moved next to alawein.
- **PyYAML in alawein** — Document that `sync-readme.py` needs PyYAML when `profile-from-guides.yaml` is present. Optionally add `pyyaml` to a requirements or optional-deps note in README/CONTRIBUTING so CI can install it when running profile sync.
- **Sync check behavior** — Ensure `sync-readme.py --check` only fails on profile section when `profile-from-guides.yaml` exists; when the file is missing, do not require PyYAML or profile block changes.

### Phase C — meshal-web and LinkedIn

- **meshal-web** — Add a build step or script that reads `profile-export.yaml` (or a copy dropped into meshal-web) and injects hero/bio/CTA from the export; or document manual steps. Update meshal-web README to state profile content source.
- **LinkedIn snippet** — In `_pkos`, extend `export-profile.py` to write `out/linkedin-snippet.md` (or similar) with `linkedin_headline` and a truncated `about` for copy-paste. Document in `_pkos` `templates/README.md`.

### Phase D — Optional structural improvements

- **`_pkos` move** — If user moves `_pkos` to `../GitHub/github.com/_pkos`, update HANDOFF-CODEX key paths and export default; set `ALAWEIN_REPO_PATH` if needed.
- **Notion / CV** — Done: `templates/notion.yaml` and `templates/cv-master.yaml` added; documented in `_pkos` `templates/README.md`. Export script can be extended to write `out/notion-snippet.md` / `out/cv-snippet.md` when requested.

---

## 3. Syncing and maintenance runbook

### Regular sync (after editing profile or copy in `_pkos`)

```bash
# 1. In _pkos (path may be Desktop/guides or Desktop/_pkos)
cd C:\Users\mesha\Desktop\guides   # or ...\Desktop\_pkos
python scripts/export-profile.py --write-alawein
# Optional: set ALAWEIN_REPO_PATH to alawein repo root if default path is wrong

# 2. In alawein
cd C:\Users\mesha\Desktop\GitHub\github.com\alawein\alawein
python scripts/sync-readme.py
```

### After editing `projects.json` in alawein

```bash
cd C:\Users\mesha\Desktop\GitHub\github.com\alawein\alawein
python scripts/sync-readme.py
```

### Quality gates (alawein, before commit/PR)

```bash
python scripts/sync-readme.py --check
./scripts/validate-doc-contract.sh --full
```

### Rename guides → _pkos (once, when folder not in use)

```powershell
Rename-Item -Path "C:\Users\mesha\Desktop\guides" -NewName "_pkos"
```

Then update any hardcoded `guides` paths in HANDOFF-CODEX and export script default if necessary.

---

## 4. Reference — key files

| File | Repo | Purpose |
|------|------|---------|
| [profile-sync-from-guides.md](profile-sync-from-guides.md) | alawein | Flow, files, quality gates, `_pkos` naming. |
| [AGENTS.md](../../AGENTS.md) | alawein | Boundaries; profile sync mentioned. |
| [CLAUDE.md](../../CLAUDE.md) | alawein | Governance sources; profile sync link. |
| sync-readme.py | alawein | Projects + profile sync into README.md. |
| export-profile.py | _pkos | Build profile-export.yaml and optional profile-from-guides.yaml. |
| profile-copy.yaml | _pkos | Tagline, bios, headline, variant overrides. |
| HANDOFF-CODEX.md | _pkos | PKOS layout, profile/positioning sync, rename. |
| templates/README.md | _pkos | How platform templates and export work. |

---

## 5. Agent checklist for “revision and enhancements”

- [x] Run Phase A (wording, templates README, doc contract, backup stub).
- [x] Run Phase B (export path, PyYAML doc, sync-check behavior).
- [x] Phase C LinkedIn snippet: `export-profile.py` writes `out/linkedin-snippet.md`; documented in `_pkos` `templates/README.md`. meshal-web consumption still manual/optional.
- [x] Phase D Notion/CV: added `templates/notion.yaml` and `templates/cv-master.yaml`; documented in `_pkos` `templates/README.md`. `_pkos` move remains user-only when desired.
- [x] Re-run `sync-readme.py --check` (passed). Run `validate-doc-contract.sh --full` locally when needed.
- [x] Update this handoff’s “last_updated” and “Not yet done” when completing work.
