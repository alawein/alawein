---
type: audit
status: superseded-in-part
last_updated: 2026-09-11
owner: meshal
---

**Status as of 2026-09-11.** PR #252 merged to `main` as `a7562deb` on
2026-09-10. G1, G2, G3, G4, G13, G14, and G22 are on `main`. PR #254
merged as `3b597f14` on 2026-09-11 with the solo-maintainer merge policy.
J17 and J18 are closed. The rest of this file is a dated read from
2026-09-10, kept as a record, not live status.

# Control-gap judgement (Cursor Cloud, 2026-09-10)

**Status:** Cursor verdict. Not Meshal acceptance. Not a second constitution.
**Executor:** Cursor Cloud on `alawein/alawein`, Slack DM.
**Independent review:** not performed in this file. Claude Code red-team is
the intended next reviewer.
**Decision owner:** Meshal Alawein (`contact@meshal.ai`).

This is the decisive read of the attached packets plus git and the three
named Slack canvases. IDs refer to
[`2026-09-10-control-gap-source-index.md`](2026-09-10-control-gap-source-index.md).

Numbered clauses **J01-J24** are the grill surface. Affirm, revise (quote
plus replacement), or reject with a native-ID contradiction. Do not replace
this file with a new "MAIOS Finalized Workflow."

## Verdict

The failure is a control gap, not a missing IDE.

Policies, prompts, permissions, deployed versions, and real executions
disagree. Each front end (ChatGPT, Gemini, Claude Code prompt, Sider,
Cursor Cloud) then writes another operating model. That is how drift
compounds.

**Keep one thin control plane in git.** Native systems stay authoritative.
[PR #252](https://github.com/alawein/alawein/pull/252) is the land path for
admission, envelopes, receipts, and recovery. Slack canvases stay pointers.
Chat threads stay analyst scratch.

## Numbered clauses

### J01. One thin control plane

Keep admission, envelopes, receipts, and recovery in
`docs/governance/control-plane.md` (G1) after #252 lands. Do not add a
parallel canon file, wiki, or Slack-canvas constitution.

### J02. Native systems win

GitHub owns code, CI, PR, and merge. Gmail and Calendar own career and
inbox commitments. 1Password / GitHub secrets own credentials. Linear owns
work state only after a recorded cutover. Slack owns coordination events,
not policy.

### J03. Canvases are pointers

C1, C2, C3, C4 are working pointers. A canvas fold is not acceptance.
Refresh only if Meshal names the canvas id. Do not @Cursor on a canvas.

### J04. Reject SoR swaps

Reject any packet that makes `ops-shared-inventory` or `workspace-brain`
`knowledge/objects/` win over git. G19 already treats the Desktop YAML as
comparison input, not a writer.

### J05. Reject conversation memory as state

ChatGPT dashboards, Composer threads, and Sider notes are scratch. Archive
once facts live in git or the owning native system.

### J06. Slack Cloud Agents stay in use

This DM and PR #252 already are Slack-launched Cloud Agent work. Reject
A5's "skip Cloud Agents" and "no Cloud Agent PRs from Slack until you turn
that on." Archive idle `Canvas user mention` agents in the human UI.

### J07. Reject a fifth workflow SSOT

A4 asks Claude Code to emit "MAIOS Finalized Workflow v1.0." Replace A4
with
[`../handoffs/2026-09-10-claude-code-fable-redteam-prompt.md`](../handoffs/2026-09-10-claude-code-fable-redteam-prompt.md).
The job is grill, not authorship.

### J08. Hashes are not acceptance

A content hash cannot be the only verify or accept signal. A1 ended *BLOCK*
on `Pasted text` vs `Pasted%20text`; hash matched; write skipped. G1 must
keep independent readback.

### J09. Independently credentialed agents are not `enforced`

Control levels are `observed`, `managed`, `enforced`. An agent with its own
keys cannot be labeled `enforced`.

### J10. No new orchestrator to close the gap

Do not install a new Slack bot, memory product, or workflow engine to
"solve" drift. Reuse git, G8, G16.

### J11. Sider hygiene only

Accept on Windows, human: fewer open Cursor folders; Composer threads are
scratch; one Cursor rule pointer to `AGENTS.md`; do not duplicate the
ChatGPT spine (`00/10/20/30/40`) inside Cursor; park `maios-dashboard`
until a named done-when.

### J12. Gemini 4-tier is advisory

Keep the sorting test (gotcha vs skill vs ADR vs strategy). Keep
`AGENTS.md` thin (102 lines on `main` at judgement time). Do not stand up
a Tier 4 central MAIOS wiki. Cross-repo research already has
`docs/internal/` (exempt) and Notion for strategy.

### J13. A3 is a critique checklist, not policy

A3 copies the #252 lifecycle, then adds Gemini tiers, a 30/60/90 plan,
Gmail/Notion `full_access` to `ask_before_writes`, and B1/B2 hash batches.
If a field in A3 is stricter and true (expiry abort-before-write,
independent readback), fold that one field into G1 on a later PR. Do not
land A3 as a parallel file.

### J14. A1 is analyst-only and stays out of git

The excluded ChatGPT export started as a career dashboard, failed to
retrieve its own last 10-20 career chats (*BLOCK*), built state from Gmail
fragments, asked to hand work to Claude Code then did the critique itself,
then became a workspace inventory, then a MAIOS harness audit, then hash
packets aimed at `AGENTS.md`. Close it for control-plane work. Do not
rerun the packets.

### J15. Naming lock

- System: **MAIOS**. **MAI** is drift unless git proves an exception.
- Company: **Kohyr**. Org and this repo: **alawein**.
- Retired hub: **alawein-hub**. Historical clone paths in old plans stay
  archive.
- Kit: **AGENT.md 1.7.0**. Changelog rows that say 1.6.0 are history
  (G7 v1.4.3, G12 v1.4.0), not a live kit rollback.
- Account: **contact@meshal.ai**.
- `@ChatGPT` is replaced. `@Grok` is not a Slack bot.
- `Astra` in the missing `gpt-6-astra` handoff is a filename *GAP*, not a
  model slug. Resolve model slugs from the live OpenRouter list, then from
  G15.

### J16. Canvas drift is proved and not self-healing

C1 still lists draft #232 and #239 as open. Both closed 2026-09-09.
C3 still claims session-log dirt, leftover probe branch in use, only #249
open, and `main` 19 behind. This Cloud `main` at judgement time was
`9287a0ca` (merged #249). Open PRs were #250, #251, #252.
C2 empty Slack lanes stay empty. Do not retag.

### J17. Loaded vs committed is proved

On this Cloud checkout, G1 exists on PR #252, not on branch tip `main`
`9287a0ca`. That mismatch is the control gap in one fact. Do not pretend
`control-plane.md` is already law on `main`.

### J18. Merge #252 is Meshal exact yes

CI green at `bfb49cbf` does not accept the contract. Independent review
(ChatGPT or Codex) was *GAP* at judgement time. Do not merge from this
verdict. [Closed 2026-09-10: #252 merged as a7562deb under the
solo-maintainer policy.]

### J19. OpenRouter is a grill tool, not a voter

G15/G16 may call a live panel. A 7-3 split does not beat a git
contradiction. If the key is missing, mark the panel *BLOCKED* and finish
locally. Never print the key.

### J20. Leftover probe branch is not live work

`cursor/integration-probe-1170` (closed #239) is leftover. Delete after
Meshal confirms. Do not revive `probe_integrations.py` onto `main`.

### J21. No sixth estate-audit chat

Do not start another front-end operating-model thread. Route remaining
questions to this packet, #252, or a named canvas refresh.

### J22. A5 dashboard rows are unverified from Cloud

Sider reported GitHub `alawein`+`kohyr`, Linear connected, Sentry
connected, Slack Cloud Agents present, getting-started 3/4. Those rows
are *UNVERIFIED* from this VM. Do not promote them to inventory.

### J23. Personal matters stay off git

A1 contains PII. Analyst-only. If a reviewer sees it, use it only to
confirm the drift pattern. Do not quote personal names, employers, or
personal facts.

### J24. Non-authorization

This judgement does not authorize merges, OAuth, Slack archive, new bots,
Gmail/Notion permission changes, B1/B2 writes, kernel-sync, Cloud
environment expansion to the fleet, or canvas edits.

## Authority map (resolved)

Snapshot of the 2026-09-10 read. `docs/governance/control-plane.md` and
`docs/governance/work-record-taxonomy.md` govern; where they differ, they
win.

| Field | Winner | Loser |
| --- | --- | --- |
| Code, CI, PR, merge | GitHub | Chat, canvas, Desktop inventory |
| Control-plane policy | `docs/governance/control-plane.md` after #252 | A3 unified draft, A4 fifth workflow, C1 as living law |
| Agent inventory | `catalog/agent-integrations.yaml` | Lane canvas tables, Sider dashboard notes |
| Work state | GitHub Issues until recorded Linear cutover | Notion Master Tasks, ChatGPT dashboards |
| Secrets | 1Password / GitHub secrets | `mcp.json`, envelopes, Slack |
| Windows Desktop adapter | `ops-shared-inventory` as inbox only | Using it as git SSOT (Sider) |
| Personal commitments, Kilo Linux notes, Slack channel routing | Out of scope for this repository | Not tracked here |

## Packet-by-packet

### A1 the excluded ChatGPT export

Started as a career dashboard. Could not retrieve its own last 10-20
career chats (*BLOCK*). Built state from Gmail fragments. Asked to hand
work to Claude Code and then did the critique itself. The same thread
became a workspace inventory, then a MAIOS harness audit, then hash
packets aimed at `AGENTS.md`. Last receipt: *BLOCK* on
`Pasted text` vs `Pasted%20text`; hash matched; write skipped; one
attempt consumed.

**Judgement:** J14 + J23. Analyst front end only.

### A2 Gemini practice library

No filesystem access. Inferred MAIOS and Kohyr. High-signal claim: root
instruction files fail when they become a wiki. 4-tier model is a useful
sorting test. Confidence percentages in that report are asserted, not
derived.

**Judgement:** J12. Keep the sorting test. Do not implement a new central
KB, eval platform, or "MAIOS core repo" `AGENTS.md` rewrite from this
paper.

### A3 Unified operating model (2026-09-10 draft)

Copies the #252 lifecycle and control levels, then adds Gemini tiers, a
30/60/90 plan, Gmail/Notion `full_access` to `ask_before_writes`, and B1/B2
hash batches. Status: proposed, pending authorization. It calls itself a
Tier 4 artifact.

**Judgement:** J13. Useful as a critique checklist against #252. Not
policy.

### A4 Claude Code synthesis prompt

Read-only mandate. Asks for "MAIOS Finalized Workflow v1.0" from A1+A2+A3.
Forbids executing B1/B2. Still produces a fifth constitution if the
output is treated as SSOT.

**Judgement:** J07. Replace A4 with the red-team prompt.

### A5 Sider Operator (Linux workspace-brain)

Honest boundary: no Windows Composer titles. Dashboard as Meshal Ultra:
GitHub `alawein`+`kohyr`, Linear connected, Sentry connected, Slack Cloud
Agents present, getting-started 3/4. Those dashboard rows are *UNVERIFIED*
from this VM (J22).

**Keep:** J11.

**Reject:** J04, J06, and `maios-dashboard` as an always-open workspace.

## What actually ran here (loaded vs committed)

- Adapter: Cursor Cloud, Slack-launched.
- Environment: Personal, `alawein/alawein` only. No sibling checkouts.
- Model observed: Grok 4.6 on earlier turns in this DM. This turn is
  Cursor Grok 4.6 class. Not a capability grant.
- Kit loaded: AGENT.md 1.7.0 from the checkout.
- Policy: control-plane.md exists on PR #252, not on this branch tip
  (`main` `9287a0ca`). Loaded-vs-committed mismatch: *PROVED* (J17).
- Windows Desktop, Claude Code disk, Codex Slack replies: *GAP*.

## Sequenced next actions (Meshal)

1. Review and exact-yes merge #252, or name the reject (J18). [Closed
   2026-09-10: #252 merged as a7562deb under the solo-maintainer policy.]
2. Accept Sider folder/chat hygiene on Windows. Do not change git SoR
   (J11, J04).
3. Paste the red-team prompt into Claude Code Fable 5.1 ultracode. Attach
   A2, A3, A4, A5. Do not attach A1 to a repo workspace (J14, J23).
4. After Claude Code returns, land only accepted deltas on G1 or G8. One
   PR. One executor.
5. Refresh C1/C3 only if you name those canvas ids (J03, J16). Delete
   leftover branch `cursor/integration-probe-1170` after you confirm
   (J20).

Do not start a sixth estate-audit chat (J21).

## Residual risks (do not treat as authorization)

- #252 merged to `main` as `a7562deb` on 2026-09-10 under the
  solo-maintainer policy. At judgement time it was draft; it is now law.
- Codex Slack `needs_auth` remains. Alignment across ChatGPT / Codex /
  Claude Code is via shared native records, not shared memory.
- G15 `lastVerified` 2026-08-27. Live OpenRouter ids may have moved.
  The red-team must resolve ids today, not from this file.
- Idle Cloud Agents spawned by canvas @Cursor still sit in the human UI.
