---
type: derived
source: docs/internal/specs/2026-05-11-voice-guide-source-first-refactor-design.md
sync: none
sla: none
authority: plan
audience: [contributors, agents]
last_updated: 2026-05-11
---

# Voice guide source-first refactor — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refactor the unified voice guide (currently a Desktop file generated by a Desktop assembler that reads a mix of Dropbox and GitHub paths) into a single source-of-truth artifact in `alawein/docs/style/voice-unified.md`, generated by a version-controlled assembler from four source files in the same directory, with no Yosemite/Mercor leak and full doctrine compliance.

**Architecture:** Four block source files (`VOICE.md` unchanged, plus three new files for Blocks 2/3/4) live in `alawein/docs/style/`. A new Python assembler at `alawein/scripts/ops/build_voice_unified.py` reads them, strips frontmatter, drops leading block-naming H1s, demotes remaining headers one level, synthesizes block headers from a `BLOCKS` config, and writes `alawein/docs/style/voice-unified.md` with computed `last_updated` frontmatter. The Yosemite voice guide stays at `alawein/jobs-projects/mercor/projects/project-yosemite/guides/voice_and_authorship.md` as a self-contained per-project artifact.

**Tech Stack:** Python 3.12, `unittest` (matches existing `scripts/tests/` layout), `git` for `last_updated` derivation. Markdown source files. No external dependencies.

**Spec deviation note:** The spec uses `build-voice-unified.py` (kebab-case) but the global CLAUDE.md mandates `snake_case.py` for Python files. This plan uses `build_voice_unified.py`.

**Branch and PR strategy:** Single feature branch `feat/voice-guide-source-first-refactor` from `origin/main`. Multiple commits (frequent commits within the branch). One PR at the end.

---

## File structure

**Files created:**

- `alawein/docs/style/voice-software-register.md` — Block 2 source (§7–§14)
- `alawein/docs/style/voice-surfaces.md` — Block 3 source (§15–§19, §21; no §20 Yosemite)
- `alawein/docs/style/voice-workflow.md` — Block 4 source (§22–§25)
- `alawein/docs/style/voice-unified.md` — generated output, do not hand-edit
- `alawein/scripts/ops/build_voice_unified.py` — assembler script
- `alawein/scripts/tests/test_build_voice_unified.py` — unittest suite
- `alawein/scripts/tests/fixtures/voice_unified/` — synthetic source files for tests

**Files modified:**

- `alawein/docs/style/VOICE.md` — only if §4 evidence enhancement (Task 9) decides Block 1 is the right home for physics-paper-register examples
- `alawein/scripts/doctrine/voice-check-config.yaml` (or equivalent) — add path skip for `docs/style/voice-unified.md`

**Files deleted (after cutover):**

- `C:\Users\mesha\Desktop\Meshal-Alawein-Voice-Export.md`
- `C:\Users\mesha\Desktop\_assemble_voice_export.py`
- `C:\Users\mesha\Desktop\voice-export-part4-tailoring.md`
- `C:\Users\mesha\Desktop\voice-guide-enhancement-prompt.md`

**Files left unchanged:**

- `alawein/docs/style/VOICE.md` (Block 1 source — except possibly for §4 enhancement)
- `alawein/jobs-projects/mercor/projects/project-yosemite/guides/voice_and_authorship.md` (no longer referenced by central assembler; stays as project-local artifact)

---

## Task 1: Setup — create feature branch and commit the spec

**Files:**
- Modify: working tree branch state
- Create: commit containing `alawein/docs/internal/specs/2026-05-11-voice-guide-source-first-refactor-design.md` on the new branch

**Context:** The spec was written during brainstorming and exists as an untracked file on the current branch (`chore/repo-cleanup-2026-05-05`). We need it on a clean branch so the PR contains the spec + plan + implementation as one coherent change set.

- [ ] **Step 1: Verify current state**

```bash
cd /c/Users/mesha/Desktop/Dropbox/GitHub/alawein/alawein
git status --short
```

Expected output includes:
```
?? docs/internal/specs/2026-05-11-voice-guide-source-first-refactor-design.md
?? docs/internal/plans/2026-05-11-voice-guide-source-first-refactor-plan.md
```

If the working tree has other modified files (e.g., `M docs/operations/session-log.md`), stash them before continuing:

```bash
git stash push -m "wip-pre-voice-refactor" -- docs/operations/session-log.md
```

- [ ] **Step 2: Fetch and create branch from origin/main**

```bash
git fetch origin
git checkout -b feat/voice-guide-source-first-refactor origin/main
```

Expected: branch created, working tree now reflects origin/main with the two untracked spec/plan files preserved.

- [ ] **Step 3: Commit spec and plan**

```bash
git add docs/internal/specs/2026-05-11-voice-guide-source-first-refactor-design.md docs/internal/plans/2026-05-11-voice-guide-source-first-refactor-plan.md
git commit -m "docs(internal): add voice guide source-first refactor spec and plan"
```

- [ ] **Step 4: Verify**

```bash
git log --oneline -1
git status
```

Expected: clean working tree, one commit on the new branch.

---

## Task 2: Create voice-software-register.md (Block 2 source)

**Files:**
- Create: `alawein/docs/style/voice-software-register.md`

**Context:** Block 2 currently lives as the `BLOCK2` triple-string constant inside `C:\Users\mesha\Desktop\_assemble_voice_export.py` (lines ~73–179). We extract it verbatim to a markdown file with frontmatter. Headings stay at their original level — the assembler will demote them on read.

- [ ] **Step 1: Write the file**

```markdown
---
type: canonical
source: none
sync: none
sla: none
authority: canonical
audience: [contributors, agents]
last_updated: 2026-05-11
---

# Block 2 · Design-Defense Register

_Applies to `[software-doc]` and `[notebook]` surfaces._
_Not active for physics papers, READMEs, or prompt kits._

## §7 Contribution-staking

Two required moves when describing any design decision:

**1. Direct claim:** state what is contributed, not what is done.
Bad: "The placement of the canary check ensures early detection of instrumentation failures."
Good: "Before scoring real traces, I run three synthetic traces with known outcomes. The check itself is small. The placement is the contribution."

**2. Negative scope:** state what is not being claimed.
Bad: "The canaries validate the pipeline."
Good: "None of this tests the model's general capability. The canaries test the instrumentation: the scorer, the auth gate, and the validation gate, all against fixed inputs with fixed expected outputs."

## §8 Concept naming

Bold on first introduction only. Never again.
Pattern: **two-word-to-four-word noun phrase** that names the decision or failure mode.
Rule: if you cannot name it in 2–4 words, the concept is not yet scoped.

Examples: **two-layer bleed problem**, **synchronous canary pre-run**, **diversity filter**, **conjunctive gate**, **schema separation constraint**.

## §9 Novelty discipline

Every system description includes both beats:

**What is useful but not novel** — names the standard tools and prior work in use.
Example: "KDE threshold estimation, LOO cross-validation, and Holm-Sidak FWER correction are all standard. The contribution is the scoring architecture that requires these checks before a checkpoint can promote."

**What is missing** — states the honest scope gap.
Example: "I have not found a clean way to attribute failure when V2 and V3 both fire simultaneously. The trace is dropped either way, but the diagnosis is ambiguous."

Omitting these beats is a claim that everything described is novel and complete.

## §10 Threshold discipline

State every threshold in full, every time it appears. No forward or backward references.

Good: "The extractor re-runs k = 20 times at temperature T = 0.3."
Good: "The LOO threshold requires n ≥ 200 samples."
Good: "Bootstrap CI spread is approximately 1.3 log-likelihood units at n = 30 and approximately 0.46 at n = 500."

Banned: "the threshold above", "the standard value", "the configured parameter",
"as described earlier", "the usual k".

## §11 Honest limitation voice

First person. No passive hedging.

Good: "I have not found a clean way to handle the case where V2 and V3 fire simultaneously."
Good: "This is also the only point in the pipeline that has no automated test."
Good: "I disagree: the FDR correction is wrong here because the gates are not independent."

Bad: "It should be noted that the current approach may not fully address all edge cases."
Bad: "Future work could explore improvements to this component."

## §12 "What to notice" pattern

One sentence immediately before any cell with live output, a figure, or a table.
This is a cue, not a caption. States what matters in the output and why.

Format: "What to notice in the output below: [specific element] and [why it matters]."

Good: "What to notice in the output below: the three canary 4-vectors against their expected values, and the halt message on the third."
Good: "What to notice: the KDE threshold moves by less than 0.02 between n = 200 and n = 500 — the floor matters, but the ceiling is flat."

Appears in the markdown cell immediately preceding the output cell — not after.

## §13 Callout box format

Use HTML `<div>` tags inside markdown cells. Never markdown blockquotes —
they render inconsistently across JupyterLab, VS Code, and nbviewer.

Design note (amber):
```html
<div style="background:#fdf6e3;border-left:3px solid #b08800;padding:10px 14px;margin:12px 0">
<strong>Design note.</strong> [Why this choice was made and what constraint it enforces.]
</div>
```

Known limitation (red):
```html
<div style="background:#fff5f5;border-left:3px solid #b85450;padding:10px 14px;margin:12px 0">
<strong>Known limitation.</strong> [Specific gap, honest scope, and what would fix it.]
</div>
```

## §14 The X-is-the-Y sentence

For every gate, constraint, or architectural decision: one sentence, claim first, reason after.
Pattern: "[Decision] because [reason]."

Good: "The gate is conjunctive because each validator detects a failure mode the others cannot."
Good: "The separation is enforced at runtime, not just documented, because a schema validator runs on every $\hat{I}$ before it reaches the scorer."
Good: "I use Holm-Sidak rather than FDR because the gates are safety-critical."

Banned opener: "In order to [achieve X], we [do Y]."
Rewrite as: "[We do Y] because [reason for X]."
```

- [ ] **Step 2: Verify file**

```bash
wc -l docs/style/voice-software-register.md
head -15 docs/style/voice-software-register.md
```

Expected: ~100 lines, starts with `---` frontmatter, then `# Block 2 · Design-Defense Register`.

- [ ] **Step 3: Commit**

```bash
git add docs/style/voice-software-register.md
git commit -m "docs(style): add voice-software-register.md (Block 2 source)"
```

---

## Task 3: Create voice-surfaces.md (Block 3 source, no §20)

**Files:**
- Create: `alawein/docs/style/voice-surfaces.md`

**Context:** Block 3 currently spans two locations in the existing assembler: §15–§16 in `voice-export-part4-tailoring.md` (lines 1–77 of that file, but skipping the leading `---` separator) and §17–§19, §21 in the `BLOCK3_SURFACE_ADJUSTMENTS` and `BLOCK3_PROMPT_KIT` Python constants (assembler lines ~190–262). Combine into one file. **Do not include §20 Yosemite content** — it is intentionally dropped from the central guide per spec §4.

- [ ] **Step 1: Write the file**

```markdown
---
type: canonical
source: none
sync: none
sla: none
authority: canonical
audience: [contributors, agents]
last_updated: 2026-05-11
---

# Block 3 · Surface Adjustments

_Switch in the section that matches the surface tag. Block 1 always applies._

## §15 Software / AI systems docs

Applies when surface tag is `[software-doc]`.

**Block 2 (§7–14, Design-Defense Register) is fully active on this surface.**

### First-person design authority

Use first-person for every design decision. The design has an author.

Bad: "The scorer is isolated from raw observations."
Good: "I do not let the scorer touch raw observations directly."

Bad: "Care was taken to separate the inferred state from the observation layer."
Good: "I treat the separation as a hard schema constraint, not a documentation note."

### Section structure: motivation → mechanism → "What to notice"

Open every section with the problem or motivation. The mechanism follows.
"What to notice" (§12) appears immediately before any code output or figure.

Bad:
> The validation layer is a conjunctive gate V1 ∧ V2 ∧ V3.

Good:
> After extraction, the inferred state has to be validated before it reaches the
> scorer. The validation layer is a conjunctive gate V1 ∧ V2 ∧ V3. Each gate
> targets a different failure mode; the conjunction matters because each one, on
> its own, leaves a corresponding failure mode invisible.

### Do not summarize what just happened

After presenting a design, move to what it implies.

Bad: "So, we have now seen how the extraction layer separates observations from inferred state."
Good: "The schema constraint does not solve the threshold problem. That requires a second gate, and the choice of threshold is not obvious."

### Figure captions

Format: *Figure N. One factual sentence.* Italic. Period-terminated.
Caption states what the figure shows, not that it exists.

Bad: *Figure 2. This figure shows the two-phase pipeline with all stage labels rendered.*
Good: *Figure 2. Full pipeline with stage labels and color coding; every other figure is a zoom into one region of this map.*

## §16 Technical notebooks (Jupyter)

Applies when surface tag is `[notebook]`.

**Block 2 (§7–14, Design-Defense Register) is fully active on this surface. §15 rules also apply — in particular: first-person design authority, motivation-before-mechanism structure, and figure caption format.**

### Cell sequence

Prose motivation → code cell → "What to notice in the output below: [element] and [why]."
Never: code cell → bare output with no setup prose.

### Callout box format — HTML only

Markdown blockquotes render inconsistently across JupyterLab, VS Code, and nbviewer.
Use HTML `<div>` tags inside markdown cells. Use the exact callout format defined in §13
(Block 2) without modification — amber `#fdf6e3`/`#b08800` for design notes,
red `#fff5f5`/`#b85450` for known limitations.

### Inline LaTeX

All math in LaTeX: `$\hat{I}$`, `$n \times 4$`, `$V_1 \land V_2 \land V_3$`.
No Unicode math symbols in prose (`≥`, `∧`, `∈`) — they render inconsistently in HTML export. This restriction applies to notebook output and HTML export. In §15 software docs rendered as plain Markdown, Unicode symbols are acceptable.

### Execution counts

Execution counts must be sequential (1, 2, 3…) on a clean run.
Out-of-order counts signal the notebook was not run top-to-bottom. Fix before publishing.

## §17 Physics papers `[physics-paper]`

Block 2 partially active: §10 (threshold discipline) and §11 (honest limitation) apply.
§7–9 and §12–14 do not apply.

Feynman register: physical intuition before formalism; concrete before abstract.
Notation: bold for vectors (**k**, **r**), italic for scalars (*E*, *ε*), hat for
operators (Ĥ, V̂). Define symbols at first use. Keep subscript conventions consistent.

Derivation structure: one or two sentences of physical motivation before each major
equation. State what the quantity measures and why it appears here.

Bad: "The Berry phase is γ_n = ∮ ⟨u_{n,**k**}|i∇_**k**|u_{n,**k**}⟩ · d**k**."
Good: "Adiabatic transport of a Bloch state around a closed loop in reciprocal space
accumulates a geometric phase that depends on the path, not the speed. This Berry
phase is γ_n = ∮ ..."

## §18 README & governance docs `[readme]`

Block 2 does not apply.

No YAML frontmatter. Open with repo name and one factual sentence.
Second paragraph: what it does, for whom, how it differs from the obvious alternative.
No motivational footers. No emoji in headings. No badges for unverified state.

## §19 CLAUDE.md / AGENTS.md `[claude-md]`

Block 2 does not apply.

Write for a technical reader with zero prior context. Constraints before capabilities.
Section order: workspace identity → directory structure → governance rules →
code conventions → build and test commands.

## §21 Prompt kits `[prompt-kit]`

Block 2 does not apply.

Imperative mood. State in this order: model identity → surface → constraints.
Include a short forbidden-register list. For research contexts, include a
notation-discipline note.
```

- [ ] **Step 2: Verify file**

```bash
wc -l docs/style/voice-surfaces.md
grep -c "^## §" docs/style/voice-surfaces.md
```

Expected: ~110 lines, 6 section headers (`## §15`, `## §16`, `## §17`, `## §18`, `## §19`, `## §21`). **No `## §20`** — verify with `grep "## §20" docs/style/voice-surfaces.md` returning nothing.

- [ ] **Step 3: Commit**

```bash
git add docs/style/voice-surfaces.md
git commit -m "docs(style): add voice-surfaces.md (Block 3 source, no Yosemite §20)"
```

---

## Task 4: Create voice-workflow.md (Block 4 source)

**Files:**
- Create: `alawein/docs/style/voice-workflow.md`

**Context:** Block 4 currently lives as the `BLOCK4` Python constant in the existing assembler (lines ~264–322). Note: the §22 surface-tag table in the spec drops the `[yosemite]` row. Make sure the table in this file does not include yosemite.

- [ ] **Step 1: Write the file**

```markdown
---
type: canonical
source: none
sync: none
sla: none
authority: canonical
audience: [contributors, agents]
last_updated: 2026-05-11
---

# Block 4 · Polish Workflow

## §22 AI application instructions

| Tag | Blocks active |
|-----|---------------|
| `[software-doc]` | Block 1 + Block 2 + §15 |
| `[notebook]` | Block 1 + Block 2 + §15 + §16 |
| `[physics-paper]` | Block 1 + §17 (Block 2 §10, §11 only) |
| `[readme]` | Block 1 + §18 |
| `[claude-md]` | Block 1 + §19 |
| `[prompt-kit]` | Block 1 + §21 |

Per-project voice contracts (e.g., Mercor/Yosemite) live in each project's own `guides/` directory and are not part of this guide.

When polishing existing text: apply the relevant blocks as a checklist. Find and
fix each violation before returning output. Do not silently pass violations.

When generating new text: apply as constraints. Do not generate text that would
fail §24.

## §23 Pre-write checklist

- [ ] Surface identified and tag selected.
- [ ] Opening claim drafted before any supporting context.
- [ ] `[software-doc]`/`[notebook]`: contribution claim (§7) drafted before mechanism prose.
- [ ] `[physics-paper]`: physical motivation drafted before each major equation.
- [ ] Numbers: all values sourced independently, not inherited from LLM output.
- [ ] Forbidden register (§5) scan on draft opener.

## §24 Post-write audit

Run in order. One hit = fix before proceeding.

1. **Forbidden register scan.** Every phrase from §5 (in Block 1). One hit = rewrite the sentence.
2. **Em-dash count.** More than 2 per section: rewrite. Three or more in one paragraph: definitive.
3. **Sentence opener check.** More than half should open with a technical subject — a named quantity, entity, or operator — not a discourse marker or "The."
4. **Hedge stacking.** Two or more hedging modifiers in sequence: remove all but one.
5. **Uniform bullet check.** 4+ bullets of equal length: rewrite as prose with colons.
6. **Threshold check.** Every threshold named explicitly with its value. No "the configured value."
7. **Voice check.** Would a reader who has read your papers or notebooks recognize this as yours? If it reads cleaner or more uniform than your usual prose, investigate.

## §25 Repo-wide scan command

<!-- voice-check:ignore-start -->
```bash
rg -n "Furthermore|Additionally|It is important to note|plays a crucial|is crucial\.[^s]|delve into|robust framework|seamlessly|transformative|groundbreaking|game-changing|leverage|passionate about|excited to|it's worth noting|interestingly|deep dive|utilize|moving forward|synergy|holistic|empower|feel free to|as mentioned above" --glob "*.md" --glob "*.ipynb" --glob "*.txt"
```

PowerShell fallback:
```powershell
Select-String -Path *.md,*.ipynb,*.txt -Recurse -Pattern 'Furthermore|Additionally|It is important to note|plays a crucial|delve into|robust framework|seamlessly|transformative|groundbreaking|leverage|passionate about|excited to|deep dive|utilize|moving forward|synergy|holistic|empower|feel free to|as mentioned above'
```
<!-- voice-check:ignore-end -->

One hit in any governed surface: rewrite that sentence.
```

**Note on the `voice-check:ignore-start/end` sentinels:** They are a forward-looking marker for the planned voice-check sentinel feature. Today they are inert HTML comments. Until the sentinel feature lands (Task 8 stopgap), the path-based skip handles the linter exemption.

**Note on §22 yosemite removal:** Compare the original `BLOCK4` constant in `_assemble_voice_export.py` against the table above. The original has a `| `[yosemite]` | Block 1 + §20 |` row; this version omits it intentionally. The §22 footnote DOES mention "Mercor/Yosemite" as an example of where per-project voice contracts live, so a `grep -i yosemite` against this file returns exactly 1 hit (the footnote). Verify with `grep -ic yosemite docs/style/voice-workflow.md` returning `1`.

**Note on §23 yosemite removal:** The original `BLOCK4` `## §23 Pre-write checklist` has a `[yosemite]:` line; this version drops it. Verify with `grep "\[yosemite\]" docs/style/voice-workflow.md` returning nothing.

**Note on §24 yosemite removal:** The original `BLOCK4` `## §24 Post-write audit` has step `8. **`[yosemite]` only:** number traceability...`. This version drops it (only steps 1–7 remain). Verify the file has 7 numbered post-write audit steps with `grep -c "^[0-9]\." docs/style/voice-workflow.md` returning 7.

- [ ] **Step 2: Verify file**

```bash
wc -l docs/style/voice-workflow.md
grep -ic yosemite docs/style/voice-workflow.md
grep "\[yosemite\]" docs/style/voice-workflow.md
grep -c "^[0-9]\. " docs/style/voice-workflow.md
```

Expected: ~60 lines, exactly 1 case-insensitive yosemite hit (the §22 footnote), zero `[yosemite]` bracket-tag hits, 7 numbered audit steps.

- [ ] **Step 3: Commit**

```bash
git add docs/style/voice-workflow.md
git commit -m "docs(style): add voice-workflow.md (Block 4 source, Yosemite refs removed)"
```

---

## Task 5: Write assembler tests (TDD)

**Files:**
- Create: `alawein/scripts/tests/test_build_voice_unified.py`
- Create: `alawein/scripts/tests/fixtures/voice_unified/source_a.md`
- Create: `alawein/scripts/tests/fixtures/voice_unified/source_b.md`

**Context:** The assembler has four pure functions worth unit-testing in isolation: `strip_frontmatter`, `drop_leading_h1`, `demote_headers`, and `assemble`. `get_last_updated` runs subprocess(git) and is integration-tested against a real git checkout in a separate test. We write tests *before* the implementation (Task 6) so the implementation is driven by failing tests.

- [ ] **Step 1: Create fixtures directory**

```bash
mkdir -p scripts/tests/fixtures/voice_unified
```

- [ ] **Step 2: Write fixture `source_a.md`**

File: `alawein/scripts/tests/fixtures/voice_unified/source_a.md`

```markdown
---
type: canonical
source: none
last_updated: 2026-05-11
---

# Block A · Test Block

_Subtitle line for Block A._

## §1 First section

Body text for §1.

### Subsection of §1

More body text.

## §2 Second section

Body text for §2.
```

- [ ] **Step 3: Write fixture `source_b.md`**

File: `alawein/scripts/tests/fixtures/voice_unified/source_b.md`

```markdown
# Block B · Another Test Block

Body without frontmatter, with leading H1.

## §3 Section three

Body.
```

- [ ] **Step 4: Write the test file**

File: `alawein/scripts/tests/test_build_voice_unified.py`

```python
from __future__ import annotations

import sys
import unittest
from pathlib import Path

SCRIPTS_DIR = Path(__file__).resolve().parents[1]
OPS_DIR = SCRIPTS_DIR / "ops"
for d in (str(SCRIPTS_DIR), str(OPS_DIR)):
    if d not in sys.path:
        sys.path.insert(0, d)

from build_voice_unified import (  # noqa: E402
    assemble,
    demote_headers,
    drop_leading_h1,
    strip_frontmatter,
)

FIXTURES = Path(__file__).parent / "fixtures" / "voice_unified"


class StripFrontmatterTests(unittest.TestCase):
    def test_strips_well_formed_frontmatter(self):
        text = "---\ntype: canonical\n---\n\n# Heading\n\nBody.\n"
        self.assertEqual(strip_frontmatter(text), "# Heading\n\nBody.\n")

    def test_no_frontmatter_returns_unchanged(self):
        text = "# Heading\n\nBody.\n"
        self.assertEqual(strip_frontmatter(text), text)

    def test_unterminated_frontmatter_returns_unchanged(self):
        text = "---\ntype: canonical\n\n# Heading\n"
        self.assertEqual(strip_frontmatter(text), text)


class DropLeadingH1Tests(unittest.TestCase):
    def test_drops_first_h1_line(self):
        text = "# Block A · Test\n\nBody.\n"
        self.assertEqual(drop_leading_h1(text), "Body.\n")

    def test_preserves_subtitle_after_h1(self):
        text = "# Block A · Test\n\n_Subtitle._\n\n## §1\n"
        self.assertEqual(drop_leading_h1(text), "_Subtitle._\n\n## §1\n")

    def test_no_h1_returns_unchanged(self):
        text = "## §1 First section\n\nBody.\n"
        self.assertEqual(drop_leading_h1(text), text)

    def test_h2_is_not_h1(self):
        text = "## Subsection\n\nBody.\n"
        self.assertEqual(drop_leading_h1(text), text)

    def test_blank_lines_before_h1_skipped(self):
        text = "\n\n# Block A · Test\n\nBody.\n"
        self.assertEqual(drop_leading_h1(text), "Body.\n")


class DemoteHeadersTests(unittest.TestCase):
    def test_h1_to_h2(self):
        self.assertEqual(demote_headers("# Title\n"), "## Title\n")

    def test_h2_to_h3(self):
        self.assertEqual(demote_headers("## Section\n"), "### Section\n")

    def test_h6_to_h7(self):
        self.assertEqual(demote_headers("###### Six\n"), "####### Six\n")

    def test_only_demotes_at_line_start(self):
        text = "Inline #foo and ## bar should not change.\n"
        self.assertEqual(demote_headers(text), text)

    def test_demotes_multiple_lines(self):
        text = "# H1\n\n## H2\n\n### H3\n"
        expected = "## H1\n\n### H2\n\n#### H3\n"
        self.assertEqual(demote_headers(text), expected)

    def test_preserves_content_after_hashes(self):
        self.assertEqual(demote_headers("## §7 Contribution\n"), "### §7 Contribution\n")


class AssembleTests(unittest.TestCase):
    def test_assembles_two_fixtures_with_block_headers(self):
        blocks = [
            {
                "file": FIXTURES / "source_a.md",
                "title": "Block A · Test Block",
                "subtitle": "_Subtitle line for Block A._",
            },
            {
                "file": FIXTURES / "source_b.md",
                "title": "Block B · Another Test Block",
                "subtitle": None,
            },
        ]
        result = assemble(blocks, today="2026-05-11", last_updated="2026-05-11")

        self.assertIn("---\ntype: generated\n", result)
        self.assertIn("last_updated: 2026-05-11\n", result)
        self.assertIn("# Meshal Alawein — Voice Guide\n", result)
        self.assertIn("<!-- Generated by scripts/ops/build_voice_unified.py", result)

        self.assertIn("## Block A · Test Block\n", result)
        self.assertIn("_Subtitle line for Block A._\n", result)
        self.assertIn("## Block B · Another Test Block\n", result)

        self.assertIn("### §1 First section\n", result)
        self.assertIn("#### Subsection of §1\n", result)
        self.assertIn("### §3 Section three\n", result)

        self.assertNotIn("# Block A · Test Block\n\n", result)
        self.assertNotIn("# Block B · Another Test Block\n\n", result)

    def test_block_separator_between_blocks(self):
        blocks = [
            {
                "file": FIXTURES / "source_a.md",
                "title": "Block A · Test Block",
                "subtitle": None,
            },
            {
                "file": FIXTURES / "source_b.md",
                "title": "Block B · Another Test Block",
                "subtitle": None,
            },
        ]
        result = assemble(blocks, today="2026-05-11", last_updated="2026-05-11")
        self.assertIn("\n---\n\n## Block B", result)

    def test_idempotent(self):
        blocks = [
            {
                "file": FIXTURES / "source_a.md",
                "title": "Block A · Test Block",
                "subtitle": "_Subtitle line for Block A._",
            },
        ]
        first = assemble(blocks, today="2026-05-11", last_updated="2026-05-11")
        second = assemble(blocks, today="2026-05-11", last_updated="2026-05-11")
        self.assertEqual(first, second)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 5: Run tests to verify they fail**

```bash
python -m pytest scripts/tests/test_build_voice_unified.py -v
```

Expected: ImportError (module `build_voice_unified` does not exist yet) — this is the desired RED state for TDD.

- [ ] **Step 6: Commit**

```bash
git add scripts/tests/test_build_voice_unified.py scripts/tests/fixtures/voice_unified/
git commit -m "test(scripts): add failing tests for build_voice_unified assembler"
```

---

## Task 6: Implement assembler (build_voice_unified.py)

**Files:**
- Create: `alawein/scripts/ops/build_voice_unified.py`

**Context:** Implement the four pure functions tested in Task 5, plus `get_last_updated` (git subprocess) and a `main` that wires them together. The assembler reads four block source files from `alawein/docs/style/`, processes them, and writes `alawein/docs/style/voice-unified.md`.

- [ ] **Step 1: Write the implementation**

File: `alawein/scripts/ops/build_voice_unified.py`

```python
"""Assemble alawein/docs/style/voice-unified.md from block source files.

Reads four block source files (VOICE.md, voice-software-register.md,
voice-surfaces.md, voice-workflow.md), strips frontmatter, drops the leading
block-naming H1, demotes remaining headers one level, synthesizes block headers
from the BLOCKS config, and writes voice-unified.md with frontmatter whose
last_updated reflects the newest source file commit date.

Run from the alawein repo root: python scripts/ops/build_voice_unified.py
"""
from __future__ import annotations

import re
import subprocess
import sys
from datetime import date
from pathlib import Path
from typing import Iterable, Optional, TypedDict

REPO_ROOT = Path(__file__).resolve().parents[2]
STYLE_DIR = REPO_ROOT / "docs" / "style"
OUTPUT_PATH = STYLE_DIR / "voice-unified.md"


class BlockSpec(TypedDict):
    file: Path
    title: str
    subtitle: Optional[str]


BLOCKS: list[BlockSpec] = [
    {
        "file": STYLE_DIR / "VOICE.md",
        "title": "Block 1 · Universal Core",
        "subtitle": "_Source: `alawein/docs/style/VOICE.md`. Applies to every surface, no exceptions._",
    },
    {
        "file": STYLE_DIR / "voice-software-register.md",
        "title": "Block 2 · Design-Defense Register",
        "subtitle": "_Applies to `[software-doc]` and `[notebook]` surfaces. Not active for physics papers, READMEs, or prompt kits._",
    },
    {
        "file": STYLE_DIR / "voice-surfaces.md",
        "title": "Block 3 · Surface Adjustments",
        "subtitle": "_Switch in the section that matches the surface tag. Block 1 always applies._",
    },
    {
        "file": STYLE_DIR / "voice-workflow.md",
        "title": "Block 4 · Polish Workflow",
        "subtitle": None,
    },
]


def strip_frontmatter(text: str) -> str:
    """Strip leading YAML frontmatter (--- ... ---) if present."""
    if not text.startswith("---\n"):
        return text
    end = text.find("\n---\n", 4)
    if end == -1:
        return text
    return text[end + len("\n---\n"):].lstrip("\n")


def drop_leading_h1(text: str) -> str:
    """Drop the first non-blank line if it is an H1 (`# `).

    Also drops one immediately-following blank line for clean spacing.
    Leaves text unchanged if the first non-blank line is not an H1.
    """
    lines = text.splitlines(keepends=True)
    for i, line in enumerate(lines):
        if not line.strip():
            continue
        stripped = line.lstrip()
        if stripped.startswith("# ") and not stripped.startswith("## "):
            del lines[i]
            if i < len(lines) and not lines[i].strip():
                del lines[i]
            return "".join(lines)
        return text
    return text


def demote_headers(text: str) -> str:
    """Add one `#` to every line beginning with `#+ ` (markdown header)."""
    return re.sub(r"^(#+)(\s)", r"#\1\2", text, flags=re.MULTILINE)


def get_last_updated(repo_root: Path, files: Iterable[Path]) -> str:
    """Return the maximum git commit date (YYYY-MM-DD) across the given files.

    Falls back to today's date for any file with no git history (e.g., not yet
    committed). If no file has any history, returns today's date.
    """
    today = date.today().isoformat()
    dates: list[str] = []
    for f in files:
        try:
            rel = f.relative_to(repo_root)
        except ValueError:
            rel = f
        result = subprocess.run(
            ["git", "log", "-1", "--format=%cs", "--", str(rel)],
            cwd=repo_root,
            capture_output=True,
            text=True,
            encoding="utf-8",
            errors="replace",
        )
        out = result.stdout.strip()
        dates.append(out if (result.returncode == 0 and out) else today)
    return max(dates) if dates else today


def _process_block(spec: BlockSpec) -> str:
    """Read one block source, strip frontmatter, drop leading H1, demote headers."""
    raw = spec["file"].read_text(encoding="utf-8")
    body = drop_leading_h1(strip_frontmatter(raw))
    return demote_headers(body).rstrip() + "\n"


def assemble(blocks: list[BlockSpec], today: str, last_updated: str) -> str:
    """Assemble all blocks into the unified guide text."""
    sources_field = ", ".join(f"docs/style/{spec['file'].name}" for spec in blocks)
    parts = [
        "---\n",
        "type: generated\n",
        f"source: {sources_field}\n",
        "sync: scripts/ops/build_voice_unified.py\n",
        "sla: regenerate on source change\n",
        "authority: derived\n",
        "audience: [contributors, agents, llm-system-prompts]\n",
        f"last_updated: {last_updated}\n",
        "---\n\n",
        "<!-- Generated by scripts/ops/build_voice_unified.py from docs/style/. Do not hand-edit. -->\n\n",
        "# Meshal Alawein — Voice Guide\n\n",
        f"_Generated: {today}. Unified reference for all writing surfaces._\n",
        "_Works as an AI system prompt and as a personal review checklist._\n\n",
        "## How to use\n\n",
        "**As an AI system prompt:** paste this guide in full, then tag the target surface: "
        "`[software-doc]`, `[notebook]`, `[readme]`, `[physics-paper]`, `[claude-md]`, or `[prompt-kit]`. "
        "Apply Block 1 universally. Apply Block 2 to `[software-doc]` and `[notebook]` surfaces. "
        "For `[physics-paper]`, apply only §10 and §11 from Block 2; all other Block 2 sections are inactive. "
        "Apply the matching § from Block 3 for surface-specific adjustments. "
        "Run §24 as a final audit pass before returning output.\n\n",
        "**As a personal checklist:** use §23 before writing and §24 after.\n",
    ]

    for i, spec in enumerate(blocks):
        parts.append("\n---\n\n")
        parts.append(f"## {spec['title']}\n\n")
        if spec["subtitle"]:
            parts.append(f"{spec['subtitle']}\n\n")
        parts.append(_process_block(spec))

    return "".join(parts)


def main() -> int:
    missing = [str(spec["file"]) for spec in BLOCKS if not spec["file"].exists()]
    if missing:
        print(f"ERROR: missing source files: {', '.join(missing)}", file=sys.stderr)
        return 1

    last_updated = get_last_updated(REPO_ROOT, [spec["file"] for spec in BLOCKS])
    today = date.today().isoformat()
    body = assemble(BLOCKS, today=today, last_updated=last_updated)

    OUTPUT_PATH.write_text(body, encoding="utf-8", newline="\n")
    print(f"Wrote {OUTPUT_PATH} ({len(body):,} chars, last_updated={last_updated})")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Run tests to verify they pass**

```bash
python -m pytest scripts/tests/test_build_voice_unified.py -v
```

Expected: all tests PASS (StripFrontmatterTests: 3, DropLeadingH1Tests: 5, DemoteHeadersTests: 6, AssembleTests: 3 = 17 tests).

If any test fails, fix the implementation (not the test) until all pass.

- [ ] **Step 3: Commit**

```bash
git add scripts/ops/build_voice_unified.py
git commit -m "feat(scripts): add build_voice_unified.py assembler"
```

---

## Task 7: First clean build + doctrine validation

**Files:**
- Create: `alawein/docs/style/voice-unified.md` (generated, then committed)

**Context:** Run the assembler against the four real source files for the first time. Validate that the output passes `validate-doc-contract.sh --full` and `validate-doctrine.py`. `voice-check` failures on the forbidden-register list in §5 are expected at this point — Task 8 handles that.

- [ ] **Step 1: Run the assembler**

```bash
cd /c/Users/mesha/Desktop/Dropbox/GitHub/alawein/alawein
python scripts/ops/build_voice_unified.py
```

Expected output: `Wrote /c/Users/mesha/Desktop/Dropbox/GitHub/alawein/alawein/docs/style/voice-unified.md (NN,NNN chars, last_updated=YYYY-MM-DD)`

- [ ] **Step 2: Inspect the output**

```bash
head -25 docs/style/voice-unified.md
echo "---"
grep -c "^# " docs/style/voice-unified.md
echo "---"
grep -c "^## Block" docs/style/voice-unified.md
echo "---"
grep -i yosemite docs/style/voice-unified.md | head -5
```

Expected:
- Frontmatter at the top with `type: generated`, `last_updated: <date>`
- Single `# Meshal Alawein — Voice Guide` H1 (count = 1)
- Four `## Block N · ...` headers (count = 4)
- Exactly one yosemite mention (the §22 footnote referencing Mercor/Yosemite as a per-project example) — confirm with `grep "\[yosemite\]" docs/style/voice-unified.md` returning nothing (no §20 inclusion)

- [ ] **Step 3: Run doctrine validation**

```bash
bash ./scripts/doctrine/validate-doc-contract.sh --full
python scripts/doctrine/validate-doctrine.py .
```

Expected: both exit 0. If `validate-doc-contract.sh` fails on `voice-unified.md` frontmatter shape, compare against `VOICE.md`'s frontmatter and adjust the assembler's `assemble()` function. If `validate-doctrine.py` fails on the H1 count or H1 content, recheck the `drop_leading_h1` and `demote_headers` logic.

- [ ] **Step 4: Run voice-check (expecting failures)**

```bash
python scripts/doctrine/validate.py --ci 2>&1 | tee /tmp/voice-check-output.txt || true
```

Expected: voice-check failures on `voice-unified.md` triggered by the forbidden-register examples in Block 1's §5 and the §25 ripgrep command. Capture the output for Task 8.

- [ ] **Step 5: Commit**

```bash
git add docs/style/voice-unified.md
git commit -m "feat(style): generate voice-unified.md from source files"
```

---

## Task 8: voice-check exemption (path-based stopgap)

**Files:**
- Modify: voice-check configuration (location to be confirmed in Step 1)

**Context:** Per spec §5, the target solution is ignore-sentinel support in voice-check (option b), but that is its own ticket. The acceptable interim is a path-based skip for `docs/style/voice-unified.md` (option a). This task implements option a only.

- [ ] **Step 1: Locate the voice-check configuration**

```bash
grep -rl "voice-check\|forbidden_register\|forbidden-register" scripts/doctrine/ | head -10
find . -name "voice-check*" -not -path "./node_modules/*" -not -path "./.git/*"
find . -name "*voice*config*" -not -path "./node_modules/*" -not -path "./.git/*"
```

Find the file that defines voice-check's target paths or skip list. It will likely be one of:
- `scripts/doctrine/voice-check-config.yaml` or `.yml`
- `scripts/doctrine/forbidden-register.yaml`
- A skip list inside `scripts/doctrine/validate.py`
- A skill at `~/.claude/skills/voice-check/SKILL.md` (less likely to be modifiable here)

- [ ] **Step 2: Add voice-unified.md to the skip list**

The exact edit depends on the config format found in Step 1. Two patterns:

**Pattern A: YAML config with `skip:` list** — add `docs/style/voice-unified.md` to the list, with a comment:

```yaml
skip:
  # Generated unified voice guide; quotes the forbidden register as examples.
  # Will be replaced by voice-check ignore-sentinel support in a follow-up.
  - docs/style/voice-unified.md
```

**Pattern B: Python config inside validate.py** — add to a `SKIP_PATHS` constant:

```python
SKIP_PATHS = {
    # Generated unified voice guide; quotes the forbidden register as examples.
    # Will be replaced by voice-check ignore-sentinel support in a follow-up.
    "docs/style/voice-unified.md",
    # ...existing entries...
}
```

If the voice-check tool lives outside this repo (e.g., a global skill), do not modify it from this plan — instead, document the dependency in a follow-up issue and proceed to Step 4 with a note in the commit message.

- [ ] **Step 3: Re-run voice-check**

```bash
python scripts/doctrine/validate.py --ci
```

Expected: exit 0. If voice-check still flags `voice-unified.md`, recheck the skip-list path format (relative vs. absolute, glob vs. exact).

- [ ] **Step 4: Commit**

```bash
git add scripts/doctrine/  # or whatever config file path was edited
git commit -m "chore(doctrine): skip voice-check on voice-unified.md (stopgap; sentinels TODO)"
```

If the tool is external and could not be modified here, instead commit a placeholder issue note:

```bash
mkdir -p docs/internal/issues
cat > docs/internal/issues/voice-check-sentinels.md <<'EOF'
---
type: derived
last_updated: 2026-05-11
---

# voice-check ignore-sentinel support

Per voice guide refactor spec §5, voice-check needs `<!-- voice-check:ignore-start -->`
... `<!-- voice-check:ignore-end -->` support so docs that legitimately quote the
forbidden register (voice-unified.md, post-mortems, audit READMEs) can pass without
path-based exemptions. Until this lands, voice-unified.md is exempt by path.
EOF
git add docs/internal/issues/voice-check-sentinels.md
git commit -m "docs(internal): track voice-check sentinel support follow-up"
```

---

## Task 9: Evidence enhancements (§4, §8, §17 from PRM and thesis)

**Files:**
- Modify: `alawein/docs/style/VOICE.md` (only if §4 enhancement decides Block 1 is the right home)
- Modify: `alawein/docs/style/voice-surfaces.md` (§17 enhancement; possibly §4)
- Modify: `alawein/docs/style/voice-software-register.md` (§8 enhancement)
- Regenerate: `alawein/docs/style/voice-unified.md`

**Context:** This task pulls real quotes from the PRM paper (`https://nano.eecs.berkeley.edu/publications/PRM_2025_Theory%20of%20Rippled%20Monolayers.pdf`) and the PhD thesis (`https://escholarship.org/content/qt7vf7f1xh/qt7vf7f1xh.pdf`) to ground the existing rules in §4, §8, and §17. **Real quotes only — do not fabricate "before" versions of published text.**

If PDF fetching is unavailable in the executing environment, **stop here, commit the refactor PR (Tasks 1–8), and run this task as a follow-up plan with PDF access**. The refactor stands on its own.

- [ ] **Step 1: Fetch and read the PRM paper**

Use WebFetch (or download + local PDF parse) on `https://nano.eecs.berkeley.edu/publications/PRM_2025_Theory%20of%20Rippled%20Monolayers.pdf`. Read the abstract, introduction, and first results section. Note candidate passages for:

- **§4 (Technical claims) physics-paper register**: 5 candidate passages showing claim-first structure or honest limitation in physics writing. Narrow to 2–3.
- **§8 (Concept naming)**: 5 candidate concept names actually introduced in the paper (named effects, regimes, gates, mechanisms). Narrow to 3.
- **§17 (Physics papers)**: 5 candidate passages showing physical motivation before formalism. Narrow to 2–3.

- [ ] **Step 2: Fetch and read the thesis**

Use WebFetch on `https://escholarship.org/content/qt7vf7f1xh/qt7vf7f1xh.pdf`. Focus on the introduction and conclusion chapters (most personally authored). Note 3–5 additional candidate passages of each type to supplement PRM.

- [ ] **Step 3: Decide §4 home**

If the physics-paper register applies to all writing (paper, thesis, technical docs), the addition belongs in `VOICE.md` (Block 1). If it is specific to the `[physics-paper]` surface, it belongs in `voice-surfaces.md` next to §17.

Default decision: place §4 examples in `voice-surfaces.md` under §17 as a "Physics paper register" subsection (avoids touching VOICE.md, which has other consumers). Override only if the chosen quotes illustrate a register that genuinely applies to non-physics writing.

- [ ] **Step 4: Edit `voice-software-register.md` for §8**

Find the existing §8 examples line:
```
Examples: **two-layer bleed problem**, **synchronous canary pre-run**, **diversity filter**, **conjunctive gate**, **schema separation constraint**.
```

Replace with:
```
Examples (software): **two-layer bleed problem**, **synchronous canary pre-run**, **diversity filter**, **conjunctive gate**, **schema separation constraint**.

Examples (physics, from the PRM paper and thesis): **<name 1>** (<one-line gloss>), **<name 2>** (<one-line gloss>), **<name 3>** (<one-line gloss>).

The pattern holds across software and physics: name the decision or failure mode in 2–4 words, bold on first introduction only.
```

Substitute `<name N>` and the glosses with the actual concept names selected in Step 1.

- [ ] **Step 5: Edit `voice-surfaces.md` for §17 (and possibly §4)**

Inside §17, add a subsection at the end:

```markdown
### Examples from published work

PRM paper (`https://nano.eecs.berkeley.edu/publications/PRM_2025_Theory%20of%20Rippled%20Monolayers.pdf`):

> <quote 1 verbatim from the paper, showing physical motivation before equation>

> <quote 2 verbatim>

PhD thesis (`https://escholarship.org/content/qt7vf7f1xh/qt7vf7f1xh.pdf`):

> <quote 3 verbatim>

Each shows the Feynman-register move: state what the quantity measures and why
it appears here, then introduce the formalism.
```

If §4 was placed in `voice-surfaces.md` (per Step 3 default), add a similar subsection under a new heading `## §4-physics Physics paper claim register` immediately before §17 — but only if the quotes selected for §4 are distinctly different from §17's. If they overlap, keep them under §17 only.

If §4 was placed in `VOICE.md` instead, edit `VOICE.md` similarly under its existing "Feynman register" or "Technical claims" section.

- [ ] **Step 6: Regenerate and validate**

```bash
python scripts/ops/build_voice_unified.py
bash ./scripts/doctrine/validate-doc-contract.sh --full
python scripts/doctrine/validate-doctrine.py .
python scripts/doctrine/validate.py --ci
```

Expected: all exit 0. The `last_updated` in `voice-unified.md` should advance to today (since we just modified source files).

- [ ] **Step 7: Commit**

```bash
git add docs/style/voice-software-register.md docs/style/voice-surfaces.md docs/style/voice-unified.md
# Add docs/style/VOICE.md if §4 went there
git commit -m "docs(style): add evidence-grounded examples to §8 and §17 (PRM, thesis)"
```

---

## Task 10: Delete stale Desktop artifacts

**Files:**
- Delete: `C:\Users\mesha\Desktop\Meshal-Alawein-Voice-Export.md`
- Delete: `C:\Users\mesha\Desktop\_assemble_voice_export.py`
- Delete: `C:\Users\mesha\Desktop\voice-export-part4-tailoring.md`
- Delete: `C:\Users\mesha\Desktop\voice-guide-enhancement-prompt.md`

**Context:** The Desktop versions are now superseded by the in-repo source files and assembler. Delete only after confirming the in-repo build matches.

- [ ] **Step 1: Diff the in-repo build against the Desktop original**

```bash
diff <(grep -v "^_Generated:" "/c/Users/mesha/Desktop/Meshal-Alawein-Voice-Export.md") \
     <(grep -v "^_Generated:" docs/style/voice-unified.md) | head -100
```

Expected differences:
- The Desktop original includes Block 3 §20 (Yosemite) and the §22 yosemite row; the new version does not (intentional per spec §4).
- The Desktop original may have a `# Meshal Alawein — Voice Guide` plus `# Voice and style contract` (two H1s); the new version has one H1 only.
- The Desktop original lacks frontmatter and the warning comment; the new version has both.
- Newer evidence enhancements from Task 9 will appear in the new version only.

If unexpected differences appear (missing sections, content drift outside the above), investigate before deleting.

- [ ] **Step 2: Delete the Desktop files**

```powershell
Remove-Item "C:\Users\mesha\Desktop\Meshal-Alawein-Voice-Export.md"
Remove-Item "C:\Users\mesha\Desktop\_assemble_voice_export.py"
Remove-Item "C:\Users\mesha\Desktop\voice-export-part4-tailoring.md"
Remove-Item "C:\Users\mesha\Desktop\voice-guide-enhancement-prompt.md"
```

- [ ] **Step 3: Verify deletion**

```powershell
Get-ChildItem "C:\Users\mesha\Desktop\" -Filter "*voice*"
Get-ChildItem "C:\Users\mesha\Desktop\" -Filter "_assemble_voice_export*"
```

Expected: no results.

This task has no commit (the deletions are outside the repo).

---

## Task 11: Open PR

**Files:**
- Push branch to origin
- Open PR via `gh pr create`

**Context:** Single PR titled per the spec's commit-subject convention. Body summarizes the four source files, the new build script, the Yosemite removal, and the deferred follow-ups (voice-check sentinels, Path C surface-specific prompts).

- [ ] **Step 1: Push the branch**

```bash
git push -u origin feat/voice-guide-source-first-refactor
```

- [ ] **Step 2: Verify branch CI status (if applicable)**

```bash
gh run list --branch feat/voice-guide-source-first-refactor --limit 5
```

If CI runs on push, wait for it to complete before opening the PR. If it fails, fix before continuing.

- [ ] **Step 3: Open the PR**

```bash
gh pr create --base main --title "docs(style): unify voice guide with source-first build" --body "$(cat <<'EOF'
## Summary

Refactor the unified voice guide from a Desktop-only assembler reading mixed Dropbox/GitHub paths into a doctrine-compliant in-repo artifact with a single source of truth.

**New source files** (under `docs/style/`):
- `voice-software-register.md` — Block 2 (§7–14, Design-Defense Register)
- `voice-surfaces.md` — Block 3 (§15–19, §21; **no §20 Yosemite**)
- `voice-workflow.md` — Block 4 (§22–25)

**New assembler** at `scripts/ops/build_voice_unified.py`. Reads four block source files, strips frontmatter, drops leading block-naming H1s, demotes remaining headers one level, synthesizes block headers from a `BLOCKS` config, and writes `docs/style/voice-unified.md` with `last_updated` derived from `git log` of the newest source.

**Generated output** at `docs/style/voice-unified.md`. Committed for downstream consumers (LLM system prompts, etc.). Carries a `<!-- Generated by ... Do not hand-edit. -->` comment.

**Yosemite split.** Per-project voice (Mercor/Yosemite) stays at `jobs-projects/mercor/projects/project-yosemite/guides/voice_and_authorship.md`. Central guide §22 includes a one-line footnote.

**Tests.** `scripts/tests/test_build_voice_unified.py` covers the four pure functions (`strip_frontmatter`, `drop_leading_h1`, `demote_headers`, `assemble`) with synthetic fixtures.

**Evidence enhancements** (§4, §8, §17): grounded with quotes from the PRM paper and PhD thesis. `[Note: include only if Task 9 was completed in this PR; otherwise remove this bullet.]`

## Spec and plan

- Spec: `docs/internal/specs/2026-05-11-voice-guide-source-first-refactor-design.md`
- Plan: `docs/internal/plans/2026-05-11-voice-guide-source-first-refactor-plan.md`

## Follow-ups (deferred)

- voice-check ignore-sentinel support (current PR uses path-based skip as stopgap).
- Path C: surface-specific prompt files (`docs/style/prompts/`) for token-efficient paste.
- §11a disagreement voice (defer until a real corpus exists).

## Test plan

- [ ] `python -m pytest scripts/tests/test_build_voice_unified.py -v` passes
- [ ] `python scripts/ops/build_voice_unified.py` produces `docs/style/voice-unified.md` matching the committed file
- [ ] `bash ./scripts/doctrine/validate-doc-contract.sh --full` exits 0
- [ ] `python scripts/doctrine/validate-doctrine.py .` exits 0
- [ ] `python scripts/doctrine/validate.py --ci` exits 0
- [ ] No `yosemite` references in `docs/style/voice-unified.md`
- [ ] Raw GitHub URL `https://raw.githubusercontent.com/alawein/alawein/main/docs/style/voice-unified.md` resolves after merge (allow up to 5 min for CDN)
EOF
)"
```

Edit the body to remove the evidence-enhancements bullet if Task 9 was deferred.

- [ ] **Step 4: Verify the PR opened**

```bash
gh pr view --web
```

Or:

```bash
gh pr view
```

Expected: PR exists with the title above, base `main`, head `feat/voice-guide-source-first-refactor`.

---

## Post-merge follow-up (not a task in this plan)

After the PR is merged, verify the raw GitHub URL resolves:

```bash
curl -sI https://raw.githubusercontent.com/alawein/alawein/main/docs/style/voice-unified.md | head -3
```

Expected: `HTTP/2 200`. If `404`, wait up to 5 minutes for the GitHub raw CDN to refresh, then retry. If still 404, verify the file is on `main` with `git -C alawein log --oneline main -1 -- docs/style/voice-unified.md`.
