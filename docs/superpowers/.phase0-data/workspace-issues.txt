PHASE 0 TRIAGE — WORKSPACE-LEVEL ISSUES
Generated: 2026-04-23

==============================================================
STEP 1: ROOT package.json
==============================================================
{
  "name": "alawein-workspace-root",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace-root delegator for the Alawein repo fleet; primary build delegation targets design-system/",
  "scripts": {
    "build": "npm run build --prefix design-system",
    "test": "npm run test --prefix design-system",
    "lint": "npm run lint --prefix design-system",
    "release": "npm run release --prefix design-system"
  },
  "devDependencies": {
    "playwright": "1.59.1"
  }
}

Notes on root package.json:
- Only delegates to design-system; no workspace workspaces[] field
- Has playwright as a devDependency at root — purpose unclear (no test script using it at root level)
- No workspaces[] array, so npm workspace protocol is not active at root level

==============================================================
STEP 2: projects.json INCONSISTENCIES
==============================================================

=== DUPLICATE SLUGS ===
  edfp: appears in ['featured', 'research']
  alembiq: appears in ['featured', 'research']
  optiqap: appears in ['featured', 'research']
  ?: appears in ['packages', 'packages', 'packages', 'packages', 'packages', 'packages']

Note: The '?' slug duplicate indicates 6 package entries with no 'slug' field in the
packages section of projects.json. These are likely @alawein/* npm packages that use
a different identifier scheme.

=== IN projects.json BUT NOT ON FILESYSTEM ===
  mercor-llm-failsafe
  morphism

Note:
- 'mercor-llm-failsafe' — unknown status, not present at workspace root
- 'morphism' — this is the morphism-systems monorepo, lives at
  C:/Users/mesha/Desktop/Dropbox/GitHub/morphism-systems/morphism/
  (different workspace root, not under alawein/)

=== ON FILESYSTEM (git repos) NOT IN projects.json ===
  alawein
  design-system
  fallax
  knowledge-base
  roka-oakland-hustle
  workspace-tools

Note: These are the 6 infrastructure/governance repos that are not tracked in
projects.json as first-class slugs. They exist on disk with .git but are absent
from the registry. This is a known gap — projects.json tracks product repos
primarily, not the infra repos. However this means they are invisible to any
tooling that reads projects.json for the repo list.

=== NO-GIT DIRECTORIES AT WORKSPACE ROOT ===
  jobs-projects
  quantumalgo

Note:
- 'jobs-projects' — bare directory, no .git. Likely a non-repo collection of files.
  Not in projects.json.
- 'quantumalgo' — has SSOT.md but no .git. May be a placeholder or archived project
  that was never properly initialized as a git repo.

==============================================================
STEP 3: ROOT-LEVEL FILE ANOMALIES
==============================================================
  AGENTS.md
  CLAUDE.md
  INDEX.md
  README.md
  REPO_GOVERNANCE_INITIATIVE.md
  firebase-debug.log  <-- UNEXPECTED
  package-lock.json
  package.json
  projects.json

Note: The workspace root projects.json references "$schema": "./projects.schema.json"
but that file does not exist at the workspace root. The schema file exists only in
the alawein/ sub-repo. This is a confirmed gap — any tool resolving the $schema
reference from the workspace root will fail to find it.

UNEXPECTED FILES:
  firebase-debug.log — A Firebase debug log file sitting at the workspace root.
  This is leftover artifact from a Firebase-related operation. Should be deleted
  or added to a .gitignore. The workspace root has no .gitignore, so this file
  persists indefinitely (Dropbox-synced).

MISSING EXPECTED FILES:
  projects.schema.json — NOT present at workspace root. Confirmed: workspace root
  projects.json has "$schema": "./projects.schema.json" but this file does not exist
  here. The schema file exists only in the alawein/ sub-repo at alawein/projects.schema.json.

==============================================================
SUMMARY OF WORKSPACE ISSUES
==============================================================
1. firebase-debug.log at workspace root — stale artifact, should be cleaned up
2. projects.json does not track 6 infra/governance repos (alawein, design-system,
   fallax, knowledge-base, roka-oakland-hustle, workspace-tools)
3. projects.json references 2 non-existent slugs: mercor-llm-failsafe, morphism
4. 3 duplicate slugs in projects.json (edfp, alembiq, optiqap in featured + research)
5. 6 package entries in projects.json missing 'slug' field
6. 2 non-git directories at root: jobs-projects (unknown purpose), quantumalgo
   (has SSOT but no git)
7. projects.schema.json: workspace root projects.json has "$schema": "./projects.schema.json"
   but the file does not exist at the workspace root — confirmed broken $schema reference.
   The file exists only in alawein/ sub-repo.
8. workspace root has no .gitignore (mentioned in CLAUDE.md notes)
9. Root playwright devDependency with no clear root-level test purpose
