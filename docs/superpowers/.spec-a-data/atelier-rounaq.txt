=== REPO: atelier-rounaq ===
Generated: 2026-04-23

--- TECH STACK ---
Framework: Vite 7.3.2 / React 19
TypeScript strict: yes (strict: true in tsconfig.json)
Tailwind version: 4.x (@tailwindcss/vite 4.2.1, tailwindcss ^4)
Design system packages: none — zero @alawein/* packages
Note: zero @alawein/* packages — verdict: INTENTIONAL luxury divergence with evidence
  Evidence: src/design/ is a complete custom design system (tokens, hooks, utilities API)
  with explicit luxury palette, Arabic typography, bespoke motion tokens.
  src/index.css defines a full @theme inline + :root CSS variable layer.
  CLAUDE.md explicitly says "Preserve luxury editorial tone."
  The absence of @alawein/* is a deliberate architectural choice, not an oversight.
State management: CartContext (React Context, localStorage) — TanStack Query v5.28.0 installed but UNUSED
Data fetching: none at runtime — all product data is static (src/data/collection.ts)
  Supabase client is configured (src/integrations/supabase/client.ts) for appointments table
  but no live queries fire via TanStack Query
Live URL: https://atelier-rounaq.com
Origin: lovable-tagger present in dependencies — appears to have been scaffolded via Lovable

--- SOURCE STRUCTURE ---
src/ dirs: components/, config/, constants/, context/, data/, design/, integrations/supabase/,
  lib/, locales/, pages/, styles/, test/, types/
Routes (from src/App.tsx):
  / LandingPage
  /about AboutPage
  /atelier AtelierPage
  /booking BookingPage
  /cart CartPage
  /checkout CheckoutPage
  /contact ContactPage
  /events/:id EventDetailPage
  /portfolio PortfolioPage
  /privacy PrivacyPage
  /products/:id ProductDetailPage
  /services ServicesPage
  /terms TermsPage
  * NotFoundPage
  NOTE: /login route is absent from App.tsx but ProtectedRoute redirects to /login
  NOTE: /design-system route is in config/routes.ts but NOT wired into App.tsx

Component count: 31 TSX files in src/components/ (flat subdirectories: animations, auth,
  booking, brand, buttons, cards, layout, luxury, navigation, shared, ui + 6 root-level)
i18n: English + Arabic (i18next v25), 2 locale files, bilingual detection via localStorage/navigator
  RTL direction switching implemented in App.tsx via useEffect on language change
design/ contents: tokens.ts (color palette, typography, spacing, radius, shadow, motion),
  hooks.ts (React hooks for all token categories), index.ts (re-export barrel)
  Missing: design/utilities.ts and design/components/ — both imported but DO NOT EXIST

--- DESIGN SYSTEM ADOPTION ---
@alawein/* packages: none
Custom design system: INTENTIONAL and well-structured
  src/index.css: @theme inline Tailwind v4 + :root CSS variables (color, font, spacing,
    shadow, motion tokens) — dark luxury palette (#0a0908 bg, #c9a66b gold accent)
  src/design/tokens.ts: dual-layer token system (light tokens object at top, then a larger
    colors/typography/spacing/shadows/borderRadius/transitions/breakpoints/zIndex export below)
    WARNING: tokens.ts has a DUPLICATE export bug — exports `tokens` and `DesignTokens` twice
    (lines 1 and 52 = first pair, lines 419 and 421 = second pair). TypeScript will fail.
  src/styles/luxury.css: additional BEM utility classes (.luxury-headline, .luxury-card, etc.)
  Tailwind config: NO tailwind.config.ts/js in project root — Tailwind v4 is configured purely
    via vite plugin + @theme inline in index.css (correct for Tailwind v4 approach)
Hard-coded hex values: 85 instances across .tsx/.css/.ts files
CSS variable references: 168 instances — good ratio, CSS vars dominate
Tailwind custom tokens: yes — inline @theme defines --font-display, --font-body, --font-arabic
  :root defines --color-bg, --color-surface, --color-text, --color-muted, --color-accent, etc.
Drift verdict: INTENTIONAL STANDALONE — luxury atelier warrants bespoke dark-mode palette,
  Arabic typography stack, and editorial motion tokens that don't exist in @alawein/tokens
Evidence: src/design/tokens.ts:1, src/index.css:1-51, CLAUDE.md governance rules 1-2

--- COMPONENT QUALITY ---
Loading states: present — SkeletonLoader.tsx exists, LoadingSpinner.tsx exists,
  ProtectedRoute.tsx has spinner, BookingCalendar has pending state
Error states: present — ErrorBoundary.tsx (class component wrapper), ErrorState.tsx component
  AppointmentForm.tsx has Zod validation + sonner toast on submit error
Empty states: EmptyState.tsx exists
Form patterns: strong — AppointmentForm uses react-hook-form + Zod resolver + i18n labels
  WhatsApp number auto-fill from phone field is a thoughtful UX touch
TypeScript coverage: strict — 0 ": any" usages, strict: true, no implicit any
i18n coverage: complete parity — 151 keys in both en.json and ar.json, zero missing keys
  RTL layout managed via document.documentElement.dir in App.tsx
Key quality issue: design/utilities.ts is imported by hooks.ts (src/design/hooks.ts:25) and
  index.ts (src/design/index.ts:72) but the file DOES NOT EXIST — this will cause a TypeScript
  compilation error and build failure for any component importing from @/design

--- ARCHITECTURE ---
TanStack Query usage: INSTALLED but zero useQuery/useMutation/useInfiniteQuery calls in source.
  No QueryClientProvider wrapping in main.tsx. Installed as a dependency (^5.28.0) but
  never integrated. Likely a lovable scaffold artifact.
Context usage: CartContext manages cart[] + wishlist[] via useState + localStorage persistence
  ProtectedRoute imports useAuth from lib/auth — but lib/auth.ts does NOT EXIST (only lib/supabase.ts)
  AppointmentForm imports createAppointment from lib/booking — lib/booking.ts does NOT EXIST
Component organization: feature-grouped subdirectories (booking, luxury, layout, navigation, etc.)
  with some flat root-level utility components (SkeletonLoader, ErrorBoundary, ScrollProgress)
Dead code signals:
  - src/data/ore-showcase-data.ts: entire file is mining/ore product mock data unrelated to
    luxury fashion — DrillingRig, OreConveyor, MiningCart in MiningAnimations.tsx, ore
    category "copper-ore" Tailwind class in LuxuryCards.tsx — clear scaffold contamination
  - src/config/routes.ts: lazy-loads ClientDashboard, PreLaunchPreview, OrderHistory,
    LoyaltyProgram, Recommendations from src/components/client/ — NONE of these exist
  - src/pages/DesignSystemPage.tsx imports from '../design/components' — does not exist
  - src/config/routes.ts defines /design-system route not wired into App.tsx
  - src/components/luxury/LuxuryHero.tsx is a stub (only 21 lines, no images, no i18n)
    while LandingPage has its own inline hero — duplicate hero concept
Bundle config: Vite terser minify, manual chunks (vendor-react, vendor-i18n, vendor-ui),
  chunk size warning limit raised to 1000kb — @react-three/fiber, @react-three/drei, @react-three/xr
  are in dependencies but NO three.js usage appears in src/. These are ~2MB unneeded bundle weight.
Key architecture issue: Three build-breaking missing modules —
  1. src/design/utilities.ts: imported by hooks.ts and index.ts, does not exist
  2. lib/auth.ts: imported by ProtectedRoute.tsx, does not exist
  3. lib/booking.ts: imported by AppointmentForm.tsx, does not exist
  These will cause TypeScript and build failures if those files are included in a build pass.

--- PORTFOLIO PRESENTATION ---
Live site assessment: atelier-rounaq.com presents a dark luxury fashion atelier.
  Landing page has strong editorial intent — dark palette (#0a0908), Bodoni Moda display font,
  gold accent (#c9a66b), bilingual Arabic/English hero, collection pieces with SAR pricing.
  Framer Motion animation delays create staggered reveal. WhatsApp integration shows
  Saudi market awareness.
Target audience: Saudi / Gulf luxury fashion clientele; bilingual Arabic-English
  Private consultation booking model (not e-commerce scale), couture + bridal + evening wear
Key presentation gap: All product images reference /images/collection-*.jpg paths — these are
  local static paths with no actual images shipped. On the live site, all collection cards
  will show broken image placeholders, severely damaging the luxury brand presentation.
  src/data/collection.ts:18,24,30 — image: '/images/collection-bridal.jpg' etc.

--- FEATURE GAPS ---
1. No payment gateway integration — CheckoutPage exists but no Stripe/Moyasar/payment flow;
   luxury boutique at these price points (16,000–18,200 SAR) needs secure payment
2. No admin/CMS layer — collection pieces are hardcoded in src/data/collection.ts; adding
   new pieces requires a code deploy; Supabase is provisioned but not used for catalog data
3. TanStack Query installed but no data layer wired — Supabase appointments table has a full
   schema (types.ts) but AppointmentForm calls a lib/booking.ts that does not exist; the
   booking flow is broken end-to-end
4. No SEO/meta management — no react-helmet or equivalent; no Open Graph tags; for a
   luxury atelier whose clients discover via Instagram/search, this is a real gap
5. Client portal referenced but unbuilt — routes.ts lazy-loads ClientDashboard, OrderHistory,
   LoyaltyProgram, Recommendations but none exist; the VIP tier concept in ProtectedRoute
   has no backing implementation

--- FINDINGS SUMMARY ---
Critical: 4 | High: 4 | Medium: 2 | Low: 2

[CRITICAL] Missing design/utilities.ts causes build failure | Design | src/design/hooks.ts:25, src/design/index.ts:72 | Create utilities.ts exporting getColor, getTextStyle, getShadow, etc. OR remove the import and inline needed helpers | Effort: M

[CRITICAL] lib/auth.ts and lib/booking.ts missing — ProtectedRoute and AppointmentForm will not compile | Architecture | src/components/auth/ProtectedRoute.tsx:3, src/components/booking/AppointmentForm.tsx:8 | Implement both modules using Supabase client, or stub with no-ops if not yet active | Effort: M (auth.ts — simple auth wrapper) / L (booking.ts — Supabase write + appointment flow)

[CRITICAL] Broken product images — all collection pieces reference /images/*.jpg paths that do not exist | Portfolio Presentation | src/data/collection.ts:18,24,30 | Add actual product images to public/images/ or use Supabase Storage URLs | Effort: S

[HIGH] TanStack Query installed but fully unused — adds ~50KB bundle weight with no benefit | Architecture | package.json:37 | Either wire up useQuery for Supabase data fetching or remove the dependency | Effort: S (remove TQ entirely) / L (integrate with Supabase appointments table)

[HIGH] @react-three/fiber, @react-three/drei, @react-three/xr in dependencies but zero usage | Architecture | package.json:31-33 | Remove three.js packages — saves ~2MB potential bundle weight | Effort: S

[HIGH] Ore/mining scaffold contamination — MiningAnimations.tsx and ore-showcase-data.ts have no place in a luxury fashion codebase | Dead code | src/data/ore-showcase-data.ts, src/components/animations/MiningAnimations.tsx, src/components/cards/LuxuryCards.tsx:23 | Delete both files and replace copper-ore Tailwind class references | Effort: S

[HIGH] Client portal lazy-loads 5 components that do not exist — will throw at runtime if any protected route is hit | Architecture | src/config/routes.ts:19-25 | Implement or remove client portal stubs; /login route is also absent from App.tsx | Effort: L

[CRITICAL] tokens.ts has duplicate exports (tokens and DesignTokens declared twice) | Design | src/design/tokens.ts:52 vs 419, :1 vs 421 | Merge into a single coherent token file; the two palettes (dark editorial vs light rose/burgundy) are contradictory | Effort: S

[MEDIUM] No SEO meta tags or structured data | Portfolio Presentation | App.tsx — no helmet/meta | Add react-helmet-async or Vite-side meta for Open Graph, Arabic locale hreflang | Effort: M

[MEDIUM] LuxuryHero.tsx stub component diverges from actual hero in LandingPage.tsx | Architecture | src/components/luxury/LuxuryHero.tsx:1-21 | Either delete stub or expand it to replace the inline landing hero | Effort: S

[LOW] DesignSystemPage defined in routes.ts but not routed in App.tsx | Dead code | src/config/routes.ts:14,69 | Either expose as /design-system dev route in App.tsx or remove | Effort: XS

[LOW] src/lib/supabase.ts throws on missing env vars; src/integrations/supabase/client.ts silently uses placeholders — two parallel Supabase clients | Architecture | src/lib/supabase.ts:7, src/integrations/supabase/client.ts:12 | Consolidate to one Supabase client module | Effort: S
