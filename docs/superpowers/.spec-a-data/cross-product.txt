=== CROSS-PRODUCT SYNTHESIS ===
Generated: 2026-04-23

--- DESIGN SYSTEM ADOPTION RANKING ---
Ranked most-adopted to least-adopted of @alawein/* packages. Drift level is the findings-file rating; adoption is how completely the package surface is actually consumed.

1. scribd — Drift: Low — Packages: @alawein/tokens 0.2.0, @alawein/theme-base 0.3.0, @alawein/ui ^0.1.0 (transpiled, Button imported) — Cleanest adoption: tokens + theme imported in globals.css, @alawein/ui Button used directly, only 4 hardcoded color values (3 raw oklch literals in payment-option borders). Custom brutalist palette is an intentional override layered on top.

2. llmworks — Drift: Low — Packages: @alawein/tokens, @alawein/theme-base, @alawein/ui (Button, Card, Badge, Select, cn) — 550 var(--*) references vs 70 hex values, all hex isolated to recharts color arrays and icon strokes. Parallel local src/components/ui/ (sidebar.tsx, toast.tsx) shadow the DS but do not override it.

3. attributa — Drift: Medium — Packages: @alawein/tokens, @alawein/theme-base, @alawein/ui (~25 files, broad primitive coverage) — Good @alawein/ui breadth (Button, Card, Input, Tabs, Select, Tooltip, Progress, ScrollArea, cn). src/styles/design-tokens.css overrides theme-base with bespoke RPG neon green palette — documented enough to count as an intentional extension.

4. repz — Drift: Medium-High — Packages: @alawein/tokens, @alawein/theme-base, @alawein/ui (228 import lines across 244 files) — Heaviest @alawein/ui consumption by volume (Button 82x, Card 100+, Badge 30, cn 97). BUT tokens/theme-base packages are NOT CSS-imported; a parallel custom token layer in src/index.css is the actual visual SSOT. Dual DS.

5. bolts — Drift: Critical — Packages: @alawein/tokens 0.2.0 declared / 0.1.0 installed, @alawein/theme-base 0.3.0 declared / 0.1.0 installed, NO @alawein/ui — Tokens + theme CSS imported but immediately bypassed: 156 hardcoded Tailwind color classes vs 11 token-derived. Lockfile drift means the declared upgrade was never actually installed.

6. gymboy — Drift: High — Packages: @alawein/tokens 0.2.0 declared / 0.1.0 installed, @alawein/theme-base 0.3.0 declared / 0.1.0 installed, NO @alawein/ui (uses local shadcn copies) — Tokens imported in src/main.css then immediately overridden by a hardcoded :root block; a second competing :root block in src/index.css redefines everything again. 350 hex values, 560 var(--*) — nominally decent ratio but the var(--*) refs resolve to overridden values, not DS values.

7. meshal-web — Drift: Medium — Packages: @alawein/tokens 0.2.0 (only ninja sub-path imported in TS), @alawein/theme-base 0.3.0 installed-but-unused, @alawein/ui ^0.1.0 installed-but-unused — Zero @alawein/ui imports. Theme CSS not imported — globals.css hand-duplicates the entire 1360-line @theme block. tailwind-merge missing from cn(). Silent drift: any upstream token change is invisible here.

8. atelier-rounaq — Drift: Intentional standalone — Packages: NONE — Zero @alawein/* packages. Bespoke luxury design system in src/design/ (tokens.ts, hooks.ts, index.ts) + src/index.css @theme block. Documented in CLAUDE.md as deliberate divergence for luxury editorial tone, Arabic typography, SAR-market aesthetic. Verdict: justified. But has its own build-breaking bugs (missing utilities.ts, duplicate tokens exports).

--- SHARED COMPONENT CANDIDATES ---
Components appearing in 3+ repos that could be extracted to @alawein/ui (or, where already shipped, standardized on).

LoadingSpinner / PageLoader / Skeleton | Repos: bolts (animate-pulse inline), repz (CardSkeleton + bare "Loading..." divs), gymboy (PixelLoader, PixelSkeleton), llmworks (LoadingSpinner, PageLoader, ArenaLoadingStates), meshal-web (LoadingScreen, LoadingSpinner), attributa (LoadingView), scribd (spinner text + "Processing..."), atelier-rounaq (SkeletonLoader, LoadingSpinner) | Action: extract a headless Spinner + Skeleton primitive pair to @alawein/ui; each repo skins via tokens. 8/8 repos have their own.

ErrorBoundary / ErrorFallback | Repos: bolts, repz, gymboy (ErrorFallback untyped), llmworks, meshal-web, attributa (error-boundary.tsx), atelier-rounaq, scribd (via sonner only) | Action: extract to @alawein/ui with a token-driven default fallback and typed FallbackProps. 7/8 have hand-rolled variants with inconsistent quality (bolts' uses light-theme classes on a dark site — visually broken).

EmptyState | Repos: repz (EmptyState + EmptyStateCyberpunk + registry), gymboy (EmptyStateCard), attributa (multiple inline variants), atelier-rounaq (EmptyState.tsx), llmworks (MISSING — ResultsViewer hardcodes samples), scribd (inline "Be the first to share") | Action: extract to @alawein/ui with icon + title + description + action slot. 5/8 have their own, llmworks needs one and silently omits it.

Button | Repos: scribd, repz, llmworks, attributa all consume @alawein/ui Button; bolts, gymboy, meshal-web, atelier-rounaq maintain local Button implementations | Action: already in @alawein/ui — standardize on it. Convert bolts/gymboy/meshal-web/atelier-rounaq to @alawein/ui Button (atelier may justifiably fork for luxury variants).

Card / CardContent / CardHeader / CardTitle | Repos: repz (100+ uses of @alawein/ui), llmworks, attributa, scribd | Action: already in @alawein/ui — push remaining 4 repos (bolts, gymboy, meshal-web, atelier) to adopt.

cn utility | Repos: repz (97 uses of @alawein/ui cn), llmworks (6 imports from @alawein/ui), attributa, scribd; bolts/gymboy/meshal-web/atelier vendor their own | Action: already exported from @alawein/ui — consolidate. Note meshal-web's cn lacks tailwind-merge entirely, which is a correctness bug.

Testimonials / Social proof card | Repos: bolts (Testimonials.tsx with animate-pulse), scribd (TestimonialSection inline in page.tsx:185-251) | Action: borderline (2 repos) — not yet 3+ but both are commerce surfaces; extract if a third commerce repo ships one.

Tabs / TabsList / TabsTrigger / TabsContent | Repos: repz (16 uses), llmworks, attributa, gymboy (local shadcn copy) | Action: already in @alawein/ui — migrate gymboy local copy onto @alawein/ui.

Form primitives (Input, Label, Textarea, Select) + react-hook-form + zod pattern | Repos: repz, llmworks (ModelManager), attributa (WorkoutLogger), bolts (TestimonialForm), gymboy, atelier-rounaq (AppointmentForm), scribd (raw HTML inputs — outlier) | Action: @alawein/ui already has primitives; document the canonical react-hook-form + zod + @alawein/ui composition pattern. Scribd is the only outlier using raw inputs.

Vite boilerplate (App.css logo-spin, #646cff, .read-the-docs) | Repos: repz, llmworks, gymboy (implicit via scaffold), atelier-rounaq (via lovable-tagger) | Action: not a component — add a bootstrap lint rule to @alawein/eslint-config that flags App.css scaffold artifacts.

--- UNJUSTIFIED DIVERGENCES ---
Places where repos solve the same problem differently with no product reason.

Auth context shape | Repos: repz (src/context/AuthContext.tsx AND src/contexts/AuthContext.tsx — duplicate within one repo), attributa (AuthContext in src/features/auth vs legacy useAuth hook in Workspace.tsx), bolts (none — uses mock user), atelier-rounaq (imports from lib/auth which does not exist) | Impact: auth code diverges per-repo and even per-file within repos, blocking any shared session primitive. | Fix: ship @alawein/auth (or document the Supabase+Clerk canonical useAuth hook pattern in the workspace-tools docs).

TanStack Query declared but not used | Repos: gymboy (installed, 0 useQuery/useMutation), llmworks (QueryClient configured with production settings but zero useQuery anywhere), atelier-rounaq (installed, zero usages, no QueryClientProvider) | Impact: ~50KB dead dep per repo, manual loading state duplicated across 8+ components per repo, and inconsistent UX (every AI/data component reimplements loading/error). | Fix: workspace-level decision — adopt TQ as the canonical async state layer, or remove from the @alawein/tsconfig recommended deps. Right now each repo makes the same wrong choice.

Loading-state spinners inline | Repos: repz (bare <div>Loading...</div> in App.tsx Suspense fallbacks), scribd (inline "Processing..." string), bolts (animate-pulse div), llmworks (LoadingSpinner component) | Impact: each repo invents its own Suspense fallback; no consistent UX across portfolio surfaces. | Fix: ship @alawein/ui PageLoader + Skeleton.

Error feedback mechanism on form submit | Repos: bolts (alert() in checkout), scribd (console.error only, no UI feedback), repz (useErrorHandler hook), attributa (inline isError block), gymboy (local string state), meshal-web (controlled error state with mailto fallback — best) | Impact: user-visible error quality varies wildly across the portfolio. | Fix: adopt sonner (already in scribd deps) or a shared @alawein/ui Toast pattern, document in style contract.

Stale Vite scaffold CSS | Repos: repz (src/App.css with .logo + #646cff + logo-spin), llmworks (same), gymboy (implicit) | Impact: 42+ lines of template cruft per repo signaling "never finished cleanup" to reviewers. | Fix: add a workspace-level lint rule or a pre-commit hook that flags Vite scaffold artifacts.

tailwind-merge absence | Repos: meshal-web (explicit gap — local cn uses filter+join with no merge, so className overrides resolve by source order) | Impact: unpredictable utility class conflicts; reported as a pattern in one repo but likely present wherever local cn vendored without @alawein/ui. | Fix: export cn from @alawein/ui (done) and require consumption — repos with local cn should migrate or add tailwind-merge.

Payment/checkout fulfillment path | Repos: bolts (Stripe checkout + webhook + DB writes; but /account shows mock data), scribd (Stripe checkout session only; NO webhook; discards paymentOption/giftEmail/promoCode), repz (pricing page, no Stripe component at all), attributa (tier stubs, no processor), atelier-rounaq (CheckoutPage exists, no payment gateway) | Impact: 5/8 repos claim commercial surfaces with incomplete payment wiring. | Fix: extract @alawein/payments (or a documented Stripe webhook + entitlement module) — each repo currently reinvents or omits.

--- JUSTIFIED DIVERGENCES ---
Places where repos intentionally differ due to product type.

atelier-rounaq bypasses @alawein/* entirely | Why appropriate: luxury Saudi/Gulf fashion atelier with Arabic typography (Bodoni Moda + Arabic display stack), editorial motion tokens, and a dark luxury palette (#0a0908 bg + #c9a66b gold) that would be wrong to force into the shared DS. CLAUDE.md explicitly preserves the luxury editorial tone. Custom design/ module is well-structured.

gymboy GBA/RPG theme overrides | Why appropriate: the GBA chrome, Press Start 2P / Silkscreen pixel fonts, scanline effects, and character sprite aesthetic are the product — the retro GBA identity is load-bearing. A @alawein/theme-gymboy package is the correct endpoint, not token adoption as-is.

attributa RPG-theme extension | Why appropriate: Repz/Gymboy fitness RPG is the currently-live product; the Press Start 2P + neon green skin fits the genre. Documented override through src/styles/design-tokens.css is a legitimate pattern (though it needs explicit extension contract).

scribd brutalist palette as custom override | Why appropriate: vibrant oklch orange/blue/green/gold storefront palette is the brand identity; layered cleanly on top of tokens rather than bypassing them. Drift level stays Low because hex-literal usage is minimal.

meshal-web having no global state manager | Why appropriate: static-render portfolio site with no server state; Framer Motion handles UI state, Clerk auth is optional. Zustand/TQ would be overkill.

repz being PWA/Capacitor-configured | Why appropriate: mobile fitness coaching app genuinely benefits from offline/installable surface. Mobile surface coverage is thin but the architectural choice is sound.

bolts using hand-rolled ab-testing module | Why appropriate: commerce-focused landing page with real A/B-able funnels; a local ab-testing lib is defensible for a storefront product.

--- CRITICAL CROSS-CUTTING ISSUES ---
Issues affecting 4+ repos that need a workspace-level fix.

Issue: TypeScript strict mode disabled or partial | Repos: repz (strict: false, noImplicitAny: false, 204 `any` usages), gymboy (strictNullChecks only, 27 `any`), attributa (strict: false, noImplicitAny: false, 6 `any`), bolts (strict but uses mock data + `unknown`) — vs strict-clean: scribd, llmworks, meshal-web, atelier-rounaq | Fix: enforce strict: true as a @alawein/tsconfig base-extends requirement; add a CI gate in workspace-tools that rejects PRs loosening strict flags in consumer repos.

Issue: @alawein/tokens + @alawein/theme-base imported then overridden/neutralized | Repos: bolts (hardcoded Tailwind classes bypass tokens), gymboy (two competing :root override blocks), repz (parallel custom token layer), meshal-web (theme-base CSS not consumed at all — hand-duplicated 1360 lines) | Fix: document the three allowed relationships in SSOT.md (1) consume as-is, (2) intentional override layered on top, (3) forked DS package; any other pattern fails lint. Ship an `audit:token-adoption` script in workspace-tools that flags bypasses.

Issue: Installed @alawein/* package versions lag declared versions (lockfile drift) | Repos: bolts (declared 0.2.0/0.3.0, installed 0.1.0), gymboy (same) — and meshal-web/attributa need verification | Fix: add a pre-commit / CI check in workspace-tools that diffs package.json declared versions against node_modules installed versions for @alawein/* packages.

Issue: Dead code surfaces — orphan pages / unmounted routes / generative scaffold artifacts committed | Repos: repz (src/projects demos for unrelated products, LazyRouter unmounted, duplicate AuthContext, App.css Vite boilerplate, src/superprompts/ in src tree), gymboy (src/debug.log, 20+ claude-code-guide.jsx and IMMERSIVE_REDESIGN_GUIDE.md at repo root), llmworks (App.css boilerplate, commented init calls), attributa (7+ orphan pages Workspace/Scan/Results/Settings/Auth/Index/Documentation, duplicate Auth/NotFound pages), atelier-rounaq (ore-showcase-data.ts + MiningAnimations.tsx scaffold contamination, 5 nonexistent client portal components in routes.ts), meshal-web (stranded section components: HeroSection/ProjectsSection/AboutSection/SkillsSection never reached by active theme), bolts (blog routes with no content source, affiliate/refund TODOs), scribd (duplicate app/components vs components dirs, stale styles/globals.css) | Fix: workspace-tools script `audit:dead-routes` that finds orphan pages (exists in src/pages/ but not in router); add a `.gitignore` rule for `*.log`; add a validator for scaffold artifacts (claude-code-*.jsx, debug.log, Vite App.css boilerplate).

Issue: TanStack Query declared as a dependency but not used | Repos: gymboy (0 usages), llmworks (QueryClient configured, 0 useQuery), atelier-rounaq (0 usages, no provider) | Fix: see UNJUSTIFIED DIVERGENCES above — workspace-level decision needed.

Issue: Payment/fulfillment surfaces incomplete or deceptive | Repos: scribd (no Stripe webhook on live commercial product — Critical), bolts (admin panel unguarded + account shows mock data — Critical), attributa (tier stubs $0/$12/$24 with no processor), atelier-rounaq (CheckoutPage shell, no gateway), repz (pricing page, no Stripe integration) | Fix: ship an @alawein/payments pattern kit with reference Stripe webhook + Supabase entitlement write; treat any repo claiming commerce as blocking until it conforms.

Issue: READMEs overstate shipped features | Repos: scribd (claims "Supabase auth and entitlement delivery" — neither exists), llmworks (claims "OpenAI and Anthropic integrations where configured" — zero SDK code), repz (AI coaching platform — all stubs), attributa (attribution intelligence — live site is fitness app), bolts (partially — /account is mock), gymboy (undersells the product, opposite problem) | Fix: add a voice-contract rule in alawein's style validator that flags unverified capability claims in README files (e.g., "Stripe-backed" without a stripe webhook file; "AI-powered" without an SDK import). Dispatch the llm-prompts sweep pattern.

--- PATTERN: AI/ADVANCED FEATURE STUBS ---
Repos whose primary product claim is backed by stubs or hardcoded values.
Summary: 6/8 repos have stub or deception issues at some severity level. 5 repos (repz, llmworks, attributa, bolts, scribd) have HIGH/CRITICAL severity false product claims. atelier-rounaq is HIGH for build-breaking missing modules that back product claims. gymboy is LOW (under-sells rather than over-claims). meshal-web is the only repo with no deception signal.

repz | Claim: "AI-powered coaching platform" (WorkoutAI form analysis via TensorFlow/PoseNet, NutritionAI, predictive progression) | Actual: WorkoutAI.analyzeForm() returns hardcoded `score: 85`; predictProgress() returns static values; NutritionAI methods return hardcoded recommendations; TensorFlow/PoseNet installed but wired to nothing. Vision service directory exists with no live inference. | Severity: HIGH deception — live domain (repzcoach.com) implies working AI; any reviewer reading src/services/ai/WorkoutAI.ts sees the hardcoded return immediately.

llmworks | Claim: "LLM evaluation and security-testing workbench, runs evaluation and benchmark flows against configured providers (OpenAI, Anthropic)" | Actual: BenchmarkRunner.tsx:129 — Math.random() generates all scores; DebateMode setTimeout-returns hardcoded strings; useModelComparison.ts exports MOCK_MODELS; zero OpenAI/Anthropic SDK imports in entire src/; MMLU/TruthfulQA/GSM8K names are decorative — no datasets loaded; Supabase evaluations/models tables defined in types but never read/written; api_key_encrypted column exists but ModelManager keeps keys in useState (lost on reload); Security-testing is mentioned in CLAUDE.md but no security component exists in src/. | Severity: CRITICAL deception — "Start Benchmark, wait 154 seconds, get different results on re-run" is immediately discoverable by any technical visitor.

attributa | Claim: "Privacy-first attribution intelligence with AI detection, citation checks, and CWE scans" | Actual: live site at attributa.dev shows a fitness RPG (Repz + Gymboy), not attribution; attribution code is complete but unrouted in App.tsx. Within the attribution code: analyzeText() returns hardcoded score: 0.5/confidence: 0.8; DetectGPT curvature math is real but unwired (no model ever loads); GLTR stats implementation is correct but token ranks never computed; resolveDOI() is a stub returning fake metadata for known publisher prefixes (Nature/Science/ACM/IEEE) — no real CrossRef API call; watermark p-value math is real and correct but requires 1000+ tokens to return non-null. CWE scan is real (regex-based, Python-only, 8 rules). | Severity: CRITICAL deception — domain and description describe a product that is not deployed.

bolts | Claim: "Fitness transformation storefront with authenticated account + dashboard" | Actual: /account shows hardcoded "John Doe" + fabricated purchase history with no auth gate; /admin is publicly accessible with no auth; ProgressTracker uses hardcoded 2024-01-15 session dates; affiliate route only console.warns with TODO comment; LoyaltyProgram/ReferralProgram/BundleOffer components exist but are unwired. | Severity: HIGH deception — Stripe checkout and webhook are real, but the authenticated-user experience is fabricated and the admin panel is unprotected.

scribd | Claim: "Stripe-backed purchase and entitlement flows, Supabase-backed auth and access control" (README) | Actual: NO /api/webhooks/stripe route; success_url redirects lead nowhere; no protected downloads; paymentOption/giftEmail/promoCode sent by frontend are silently discarded server-side (all 3 UX paths resolve to identical one-time $190 charge); promo code "Apply" button calls console.log; footer /privacy, /terms, /support all 404. | Severity: CRITICAL deception — commercial live product (scribd.fit) that takes real money with no fulfillment path.

gymboy | Claim: (README undersells — opposite pattern) says "fitness coaching app" | Actual: GBA RPG, character sprite, XP/streak/level system, GBA sound effects, 9 specialized AI coaching agents, PRCelebration animations — all real and implemented. AI calls route through /api/ai with no rate limiting or caching. | Severity: LOW — this is under-selling, not over-claiming. The deception risk is zero; the portfolio risk is undervaluing the work.

meshal-web | Claim: "AI systems engineer for governed agents, evaluation infrastructure, and scientific compute" | Actual: real projects showcased (Morphism, Alembiq, LLMWorks links, EDFP, CHSH Lab, OptiQAP, REPZ), real bio with Ph.D. EECS UC Berkeley, 9 substantive blog posts, real contact form with /api/contact + mailto fallback. | Severity: NONE — coherent and defensible.

atelier-rounaq | Claim: "Luxury couture atelier with bilingual EN/AR, private consultation booking" | Actual: UI is real and polished; i18n has 151 keys parity EN/AR; booking form is built with Zod validation. BUT: lib/booking.ts that AppointmentForm imports does not exist — bookings will fail to compile; all product images reference /images/collection-*.jpg paths with no files shipped (broken images on live site); no payment gateway; client portal routes lazy-load 5 nonexistent components. | Severity: HIGH — claimed booking + commerce surface is non-functional; image assets missing damages the luxury-brand presentation immediately on landing.

--- PORTFOLIO STRENGTH RANKING ---
Note: This ranking measures portfolio defensibility (coherence, real features, no false claims) — a different axis from the Design System Adoption Ranking above. A low DS adoption rank does not imply low portfolio rank (e.g., meshal-web ranks #7 on DS adoption but #1 on portfolio defensibility).
Ranked 1 (most defensible) to 8 (least defensible) for a technical audience.

1. meshal-web — Verdict: coherent portfolio hub that tells a defensible story with real projects, real bio, real publications data, and strict TypeScript. Strength: zero Criticals, strict: true with 0 `any`, real /api/contact, 7 real projects showcased with live URLs and GitHub links. Gap: LinkedIn not surfaced in nav/footer/hero; @alawein/ui + @alawein/theme-base installed but unused (silent drift).

2. gymboy — Verdict: the strongest genuinely-functional product in the portfolio — the GBA RPG aesthetic is fully implemented and distinctive. Strength: real character progression, real AI agent system (9 agents), real PDF export, real GBA sound effects and pixel art, coherent retro identity. Gap: 3500-line App.tsx monolith, TanStack Query declared-but-unused, installed DS packages lag declared versions, README undersells the product.

3. atelier-rounaq — Verdict: visually polished luxury atelier with correct i18n/RTL and Arabic-English bilingual parity, intentional DS divergence is justified. Strength: editorial dark palette, Bodoni + Arabic typography, 151-key locale parity, WhatsApp/Saudi-market awareness, Framer Motion reveal choreography. Gap: 4 Criticals including build-breaking missing modules (design/utilities.ts, lib/auth.ts, lib/booking.ts), duplicate tokens exports, broken image paths on live site, mining/ore scaffold contamination.

4. scribd — Verdict: cleanest design-system adoption in the portfolio, visually polished commerce surface, but the core commercial promise is a stub. Strength: Low drift, @alawein/ui Button adopted, brutalist palette layered cleanly on tokens, working Stripe checkout session creation. Gap: no Stripe webhook on a live paid product (Critical), paymentOption/giftEmail/promoCode silently discarded, footer links 404, README overstates shipped entitlement delivery.

5. bolts — Verdict: real Stripe flow and real Supabase backend exist, but the authenticated user experience is fabricated and the admin panel is publicly accessible. Strength: working Stripe checkout, real webhook handler, real DB helpers in supabase.ts, content-package-driven plans, A/B testing infra. Gap: 3 Criticals — /account shows hardcoded "John Doe", /admin unguarded, lockfile mismatch (installed 0.1.0 vs declared 0.2.0/0.3.0), 156 hardcoded Tailwind colors bypass tokens.

6. repz — Verdict: largest codebase, most @alawein/ui adoption by volume, but the AI coaching product claim is entirely stubbed. Strength: 642 TS/TSX files, broad domain coverage (auth, coach tools, nutrition, metrics, photos, realtime), TanStack Query + Zustand layered correctly, PWA/Capacitor configured. Gap: 1 Critical — all AI services return hardcoded values; dead `next` dep; strict: false with 204 `any`; unrouted src/projects contamination from other alawein products; dual CSS token system.

7. llmworks — Verdict: looks credible from outside, collapses immediately on first interaction with any benchmark. Strength: polished UI, strict TypeScript, Low drift, 10 lazy-loaded routes, real ErrorBoundary, Supabase auth wired. Gap: 2 Criticals — benchmark results are Math.random(), README claims OpenAI/Anthropic integrations with zero SDK code, Supabase eval/models/analytics_events schema defined but zero reads/writes outside useAuth, DebateMode is setTimeout-scheduled hardcoded strings.

8. attributa — Verdict: the product claimed at the domain is not the product deployed at the domain. Strength: attribution code itself shows genuine domain understanding (real GLTR math, real DetectGPT curvature, real watermark p-value implementation, real CWE regex scanning); _hidden/resumeTailor is a complete production-ready module. Gap: live site at attributa.dev shows a fitness RPG, not attribution intelligence; 7+ orphaned attribution pages unreachable via App.tsx; NLP analyzer stub returns 0.5/0.8; CrossRef resolver is a prefix-matching stub; duplicate Auth/NotFound pages; strict: false; demo resume files may contain real PII.
