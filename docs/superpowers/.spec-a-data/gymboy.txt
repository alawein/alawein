=== REPO: gymboy ===
Generated: 2026-04-23

--- TECH STACK ---
Framework: Vite 7.2.6 / React 19.0.0
TypeScript strict: partial — strictNullChecks: true, but no `strict: true` flag; full strict mode not enabled
Tailwind version: v4 (via @tailwindcss/vite ^4.1.11)
Design system packages: @alawein/tokens 0.2.0 (pinned in package.json), @alawein/theme-base 0.3.0 (pinned); installed versions are 0.1.0 (stale lockfile mismatch — see Drift section)
Theme in use: Custom GBA RPG palette — "Habitica Purple-Navy" in index.css; main.css imports @alawein/tokens + @alawein/theme-base but then hard-overrides all variables with GBA-specific hex values
State management: Local React state (useState/useEffect) + IndexedDB via custom useKV hook; TanStack Query is listed as a dependency (@tanstack/react-query ^5.83.1) but has 0 actual usages in src (QueryClient/useQuery/useMutation: 0 grep matches)
Data fetching: Direct fetch() calls in src/lib/ai/agentClient.ts to /api/ai (GitHub Spark API route). No TanStack Query orchestration.
Live URL: https://gymboy.coach

--- SOURCE STRUCTURE ---
src/ dirs:
  src/animations/     — choreography.ts, micro-interactions.ts (2 files)
  src/components/     — 144 feature components + 39 shadcn/ui primitives = 183 total .tsx files
  src/components/pixel-icons/ — pixel art icon assets
  src/components/ui/  — local shadcn copies (button, card, dialog, skeleton, etc.)
  src/hooks/          — use-choreography.ts, use-form-reminders.ts, use-loading-transition.ts, use-mobile.ts, use-photo-reminders.ts, use-vital-signs-reminders.ts
  src/lib/            — 35+ utility/logic modules (types, exercises, templates, ai/, persistence/)
  src/lib/ai/         — agentClient.ts (fetch /api/ai), agentConfig.ts
  src/lib/persistence/— useKV.ts (IndexedDB with localStorage fallback)
  src/styles/         — theme.css

Routes: Single-page app — no router library. Navigation via Tabs component in App.tsx with tab state managed by useState. Approximately 10+ tab sections visible from imports (workout, nutrition, supplements, analytics, etc.)
Component count: 183 .tsx files total (144 feature, 39 ui primitives)
Notable: src/debug.log committed — contains a Chrome/Crashpad error artifact (0316/064158.595 ERROR: CreateFile failed). Root-level docs artifact sprawl: 20+ .md/.jsx files at repo root (claude-code-guide.jsx, ZELDA_POKEMON_IMMERSIVE_SUMMARY.md, IMMERSIVE_REDESIGN_GUIDE.md, etc.) suggest heavy generative dev work left in place.

--- DESIGN SYSTEM ADOPTION ---
@alawein/tokens imported: yes — src/main.css line 2: @import '@alawein/tokens'
Theme CSS imported: yes — src/main.css line 3: @import '@alawein/theme-base'
@alawein/ui components used: no (not in deps — uses local shadcn copies in src/components/ui/)
Hard-coded color values (#hex or rgb()): 350 occurrences across src/
Token-derived values (var(--): 560 occurrences across src/
Drift level: High
Evidence: src/main.css:2-3 imports @alawein/tokens and @alawein/theme-base, but both are immediately overridden — src/main.css:37 defines its own :root block with hard-coded hex values (#101828, #F0E8D0, #E04830, etc.), and src/index.css:907 redefines the same variables again with a different "Habitica Purple-Navy" palette (#1a1133, #e8deff, #b47aff). Two competing :root blocks exist. Installed package versions are 0.1.0 but package.json pins 0.2.0/0.3.0 — lockfile is stale. The design system tokens are imported but functionally neutralized by inline overrides.

--- COMPONENT QUALITY ---
Loading states: present — PixelLoader component, PixelSkeleton component, isLoading useState patterns in AI components (e.g., AIWorkoutRecommendations.tsx sets isLoading state, renders PixelLoader). LoadingScreen component in App.tsx via useLoadingTransition hook.
Error states: partial — Top-level ErrorBoundary in main.tsx wrapping entire app via react-error-boundary. ErrorFallback.tsx is styled and functional. However: ErrorFallback has an untyped props signature (error and resetErrorBoundary have no type annotations — implicit any). Component-level error states in AI components (e.g., AIWorkoutRecommendations) use local string state for errors, not structured error objects. No granular per-section error boundaries below top level.
Empty states: present — EmptyStateCard component is a dedicated reusable pattern. Dashboard.tsx PRBoardBase shows empty state when records.length === 0. Pattern is consistent in key data-display components.
Form patterns: consistent — react-hook-form + zod throughout dialog components. shadcn/ui Form primitives used uniformly.
TypeScript coverage: partial — strictNullChecks enabled but `strict: true` not set (missing noImplicitAny, strictFunctionTypes etc.). 27 `: any` usages. Notable: ErrorFallback.tsx props untyped (implicit any), App.tsx:924 updateExerciseSet uses `any`, App.tsx:1827 timing param typed as `any`, ExportPDFDialog templateStats typed as `any[]`.
Retro/progression UI claim: delivered — GBAFrame bezel chrome, PixelProgressBar, GymBoySprite (animated pixel character), RPGStatSheet (character stats with HP/MP/XP bars), LevelUpToast (level-up on every 10 workouts), AchievementUnlockNotification, PRCelebration, WorkoutSummaryScreen shows "QUEST COMPLETE!" with XP gained, streak milestone toasts, GBA sound effects (GBASounds.levelUp()), scanline/vignette CSS overlays, pixel font (Silkscreen/Press Start 2P). The "retro RPG" aesthetic is genuinely implemented, not aspirational.
Key quality issue: TanStack Query is listed as a production dependency but has zero usages in src/. All async AI calls use manual useState(isLoading) + useEffect patterns, duplicating loading/error/data state logic across every AI component. This is a significant inconsistency between declared architecture and actual implementation — App.tsx:AIWorkoutRecommendations.tsx:45-50.

--- ARCHITECTURE ---
State management pattern: All application state lives in App.tsx via useState calls backed by the useKV (IndexedDB) persistence hook. App.tsx exceeds 3,500 lines. No context providers, no reducers, no external state library in use. The entire app is one giant component that passes props down.
Data fetching pattern: AI requests go through src/lib/ai/agentClient.ts → fetch('/api/ai') → api/ai.js (Spark/Vercel serverless). All other data is local (IndexedDB). No external fitness API integrations.
Component organization: mixed — feature components are flat in src/components/ (144 files, unsorted). No feature folders (e.g., no workout/, nutrition/, supplements/ subdirectories). UI primitives separated into src/components/ui/ which is clean.
Dead code signals: src/debug.log committed (Chrome Crashpad error log — not application code, should not be in src/). vite.config.ts references `vendor-3d` chunk for `three` but three.js is not in package.json (dead chunk config). Root-level .jsx and .md files (claude-code-guide.jsx, claude-code-superprompt.jsx) are generative dev artifacts, not application code.
Bundle config: vite.config.ts implements manual chunk splitting for react, radix-ui, recharts/d3, framer-motion, tanstack-query, icons, jspdf, html2canvas. The vendor-3d chunk references `three` which is not installed — harmless but stale. chunkSizeWarningLimit: 600KB.
Key architecture issue: App.tsx monolith — all state, all handler functions, all routing logic in a single 3,500+ line file. This is the largest technical debt item. Feature state is prop-drilled through deeply nested component trees. Adding any significant feature requires touching App.tsx.
Evidence: App.tsx imports 60+ components at the top of the file and contains calculation functions (calculateStats, calculateRecoveryMetrics usage), level detection, streak detection, all dialog open/close state, all data mutation handlers — all in one component.

--- PORTFOLIO PRESENTATION ---
Live site assessment: gymboy.coach is live on Vercel (vercel.json present, dist/ exists). The GBA RPG aesthetic is distinctive and portfolio-worthy — pixel art characters, scanline effects, "GYMBOY ADVANCE" chrome, quest-themed UI language.
README quality: Minimal but functional. States the runtime model, lists dev commands, links to docs/. Does not describe the RPG mechanics, progression system, or the AI coaching features — undersells the product significantly for a portfolio context.
Key presentation gap: README makes no mention of the RPG/gamification system, the 9 specialized AI coaching agents, the GBA aesthetic, or the character progression mechanics. A developer landing on the repo sees "fitness coaching app" — not "Zelda/Pokemon-inspired RPG fitness tracker with AI coaching and pixel art character system." The visual identity is the strongest differentiator and it is completely absent from the README and any text description visible at github.com level.

--- FEATURE GAPS ---
1. No backend data persistence — all data is local IndexedDB only (no account, no sync). Users lose all workout history on device wipe or browser data clear. No export-to-cloud or sync path visible (PDF export exists but no database-backed profile).
2. No authentication or multi-user support — app is single-user local-only, making it unsuitable for any "coaching platform" use case where a coach tracks multiple athletes.
3. TanStack Query installed but unused — AI loading states are manually re-implemented in each of 8+ AI components, creating inconsistent UX (each component has its own loading/error/retry logic rather than a unified cache layer).
4. No real-time workout tracking — app has workout templates and history but no active session timer, set-completion tracking during a live workout, or rest timer integrated into a live session flow. RestTimerSettings exist but appear to be configuration-only.
5. AI agents call OpenAI directly from a Spark/Vercel edge function with no rate limiting, caching, or retry visible in api/ai.js — production AI costs are uncapped and responses are not cached (identical prompts re-generate on every component mount).

--- FINDINGS SUMMARY ---
Critical: 1 | High: 3 | Medium: 3 | Low: 2

[CRITICAL] App.tsx monolith exceeds 3,500 lines | Architecture | src/App.tsx lines 1-3500+ | Split into feature modules with context providers; extract state slices | Effort: XL

[HIGH] TanStack Query declared but never used — manual loading state duplication | Architecture | package.json + 0 grep hits for useQuery/useMutation in src | Either adopt TQ for all async state or remove the dependency and document the pattern (prerequisite for App.tsx decomposition — install TQ first, then extract components safely) | Effort: L

[HIGH] Design system tokens imported then immediately overridden — two competing :root blocks | Design System | src/main.css:2-3 (imports), src/main.css:37 (first override), src/index.css:907 (second override) | Option A: Create @alawein/theme-gymboy package (proper DS integration); Option B: Remove @alawein/tokens/@alawein/theme-base @import and own GBA palette locally. Choose one — do not leave both wired. | Effort: M

[HIGH] Installed @alawein package versions (0.1.0) lag pinned versions (tokens@0.2.0, theme-base@0.3.0) | Design System | node_modules/@alawein/*/package.json | Run npm install to resolve; verify GBA override palette still works after token updates (run visual smoke test after install — GBA overrides may silently mask token breakage) | Effort: S

[MEDIUM] src/debug.log is a committed Chrome Crashpad artifact | Repository hygiene | src/debug.log:1 | Delete file, add *.log to .gitignore | Effort: S

[MEDIUM] ErrorFallback.tsx props are untyped (implicit any) — fails strict TypeScript | TypeScript | src/ErrorFallback.tsx:4 — `({ error, resetErrorBoundary })` with no type annotation | Add `FallbackProps` from react-error-boundary | Effort: S

[MEDIUM] README undersells the product — no mention of RPG mechanics, AI agents, or visual identity | Portfolio | README.md | Add feature summary, screenshots/GIF of GBA UI, character selection, XP system | Effort: M

[LOW] Dead vite chunk config references three.js (not installed) | Build config | vite.config.ts:28 — `vendor-3d` chunk | Remove the three chunk or add the dependency if 3D is planned | Effort: S

[LOW] 20+ generative dev artifacts at repo root (claude-code-guide.jsx, ZELDA_POKEMON_IMMERSIVE_SUMMARY.md, etc.) | Repository hygiene | gymboy/ root directory | Move to docs/ or delete; root should contain only standard project files | Effort: S
