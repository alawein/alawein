=== REPO: meshal-web ===
Generated: 2026-04-23

--- TECH STACK ---
Framework: Vite 7.3.2 / React 19.0.0 with react-router-dom 7.13.2
TypeScript strict: yes — tsconfig.json "strict": true, 0 "any" usages in src/
Tailwind version: 4.x (@tailwindcss/vite 4.2.2)
Design system packages: @alawein/tokens 0.2.0, @alawein/theme-base 0.3.0, @alawein/ui ^0.1.0
Note: tailwind-merge absent from package.json — see design system adoption section for impact
State management: none — appropriate; site is fully static-render with no server state; Framer Motion covers UI state; Clerk auth is optional async-loaded chunk
Data fetching: static TypeScript data files (src/data/*.ts); contact form posts to /api/contact; no SWR/React Query
Live URL: https://www.meshal.ai

--- SOURCE STRUCTURE ---
src/
  app/             globals.css (1360 lines, Tailwind v4 @theme block, all tokens inline)
  components/      auth/, branding/, diagrams/, layout/, portal/, projects/, sections/, ui/, visualizations/
  config/          designTokens.ts, themes.ts, ninjaTokens.ts, seoManifest.js, site.ts, marketing.ts
  content/posts/   9 MDX posts (convergence-proof, dft-to-llms, distribution-shift, evaluation-is-not-testing, from-hpc-to-agent-infrastructure, governed-systems-notes, on-typed-orchestration, scoping-ai-work, what-a-governed-agent-looks-like)
  data/            bio.ts, contact.ts, cv.ts, labs.ts, projectDetailContent.ts, projects.ts, publications.ts, serviceOffers.ts, skills.ts, wit.ts
  hooks/           useScroll.ts (1 hook)
  lib/             analytics.ts, animations.ts, animationUtils.ts, authRuntime.ts, blog.ts, constants.ts, greetings.ts, imageOptimization.ts, konamiCode.ts, projectShare.ts, reportWebVitals.ts, seo.ts, swCleanup.ts, themeRuntime.ts, useScrollProgress.ts
  pages/           22 page files (Landing, Portfolio, About, Now, Resume, Services, Labs, Blog, BlogPost, Morphism, Chshlab, Neper, Edfp, Qaplibria, Repz, Llmworks, ClientPortal, ClientPortalAuthenticated, NotFound, OgPreview, ProjectPage, ThemeMiniSitePage)
  stories/         AuthNav.stories.tsx, OptimizedPicture.stories.tsx
  test/            (unit/accessibility test directory)
  themes/          editorial_noir/ (primary active theme — landing/Hero, landing/Projects, landing/Contact, landing/Pillars, landing/CastLane, portfolio/Hero, portfolio/Grid, blog/List, blog/Page, labs/Dashboard, resume/Page, services/Page, etc.), shared/, _base/
  vendor/          meshal-site-primitives/ (cn.ts, blocks.tsx, layout.tsx — local copy without tailwind-merge)

Routes (from App.tsx):
  / → Landing, /about → About, /now → Now, /projects → Portfolio, /services → Services
  /cv → Resume (/resume → redirect), /morphism → Morphism, /chshlab → Chshlab
  /neper → Neper, /edfp → Edfp, /qaplibria → Qaplibria, /repz → Repz, /llmworks → Llmworks
  /portal → ClientPortal, /labs → Labs, /blog → Blog, /blog/:slug → BlogPost
  /og-preview → OgPreview (DEV only), theme alias routes, * → NotFound

Component count: 38 TSX files in src/components/, 95 total TSX across src/
Hook count: 1 custom hook (useScroll.ts)
Root artifacts: No claude-code-guide.jsx or claude-code-superprompt.jsx found at repo root — these files referenced in the audit brief do NOT exist in the repository as of this audit. The repo root contains standard project files only (AGENTS.md, CLAUDE.md, CHANGELOG.md, package.json, etc.).

--- DESIGN SYSTEM ADOPTION ---
@alawein/tokens imported: yes — src/config/designTokens.ts imports NINJA_BRAND, NINJA_BRAND_SEQUENCE, NINJA_GRADIENTS from '@alawein/tokens/ninja'; src/config/themes.ts imports NINJA_THEMES
Theme CSS imported: no — @alawein/theme-base CSS is NOT imported in globals.css or main.tsx. All design tokens are hand-duplicated inline in src/app/globals.css (1360-line @theme block with manual hex values, spacing, radii, etc.). The @alawein/theme-base package is installed but its CSS output is unused.
@alawein/ui components used: no — zero imports of '@alawein/ui' found anywhere in src/. The package is installed (^0.1.0) but never used. All UI components are repo-local: src/components/ui/Button.tsx, Card.tsx, etc.
tailwind-merge present: no — impact: the vendor cn() at src/vendor/meshal-site-primitives/cn.ts uses a simple filter+join (Array<string | null | undefined | false>), which means conflicting Tailwind classes are NOT resolved (e.g., passing className="text-red-500" to a component that also sets "text-green-400" will emit both classes; browser applies last-defined). For a static dark-theme site with disciplined className usage this is low-severity but will cause unpredictable results if consumers pass overriding utility classes to Button, Card, or primitive layout components.
Drift level: Medium
Evidence:
  - src/config/designTokens.ts:2 — imports from @alawein/tokens (correct)
  - src/app/globals.css:1-50 — @theme block duplicates token values manually instead of @import from @alawein/theme-base
  - src/components/ui/Button.tsx — zero @alawein/ui imports; fully local implementation
  - package.json:63 — "@alawein/ui": "^0.1.0" declared but unused
  - src/vendor/meshal-site-primitives/cn.ts:1 — custom cn without tailwind-merge

--- COMPONENT QUALITY ---
Loading states: present — LoadingScreen.tsx (Suspense fallback for lazy routes), LoadingSpinner.tsx, ContactSection.tsx 'loading' submit state. Appropriate for a static site; no skeleton loaders needed.
Error states: present — ErrorBoundary.tsx wraps entire App; ContactSection has 'error' submit state with mailto fallback; 404 NotFound page exists.
Empty states: partial — Labs page documented as fixture-backed (CLAUDE.md); no explicit empty state shown for zero blog posts or zero projects (filtering by PortfolioLane has no empty-lane guard visible in Grid).
Form patterns: consistent — ContactSection uses controlled form with name/email/message/company (honeypot), proper labels, aria-live status, loading/success/error states, API-first with mailto fallback. Good a11y coverage.
TypeScript coverage: strict — "strict": true in tsconfig.json, 0 `: any` usages found in src/. No casual any usages.
Key quality issue: Button.tsx uses manual string join (Array.filter(Boolean).join(' ')) rather than tailwind-merge. When callers pass className with conflicting utilities (e.g., ContactSection passes "w-full justify-center" on top of existing SIZE_CLASSES), conflicts resolve by CSS source order only — brittle and hard to debug. File: src/components/ui/Button.tsx:83-94.

--- ARCHITECTURE ---
Portfolio content: real, not placeholder. All 7 project entries in allProjects[] (src/data/projects.ts) are actual shipped products with real milestones, GitHub URLs, live websites, and tech stacks. Data is hardcoded TypeScript — no CMS or API call. Bio and CV data in src/data/bio.ts and src/data/cv.ts are authoritative, detailed, and current.
Content coverage: partial. The 8 "active products" in projects.json are not all present. Confirmed present in meshal-web: Kohyr/morphism, Alembiq/neper, LLMWorks, EDFP, CHSH Lab, OptiQAP, REPZ, SimCore (external link). NOT present/showcased: Bolts (bolts.fit), knowledge-base, fallax/provegate, qmlab. These are either not portfolio-quality or intentionally excluded, but the gap is silent.
Navigation clarity: strong. The theme system routes cleanly; Landing → Projects → individual project pages. Each project has a dedicated route (/morphism, /neper, etc.) with full case-study content. The /cv, /services, /blog, /about, /now, and /labs pages round out the surface. Main navigation and footer are present.
Dead code signals: src/components/sections/HeroSection.tsx and src/components/sections/ProjectsSection.tsx exist but are unreachable — Landing.tsx delegates entirely to the theme system (editorial_noir/landing/*), and these section components are never imported by any theme. They are stale fallback code. Similarly, src/components/sections/AboutSection.tsx, SkillsSection.tsx may be stranded. The src/stories/ directory has only 2 stories for 38+ components.
Bundle config: manual chunk splits for vendor-react and vendor-framer in vite.config.ts. Vite cache moved to OS temp dir to avoid Dropbox EBUSY. contactApiDevPlugin provides /api/contact in local dev. MDX support via @mdx-js/rollup. Clean configuration; no unusual flags.
Key architecture issue: @alawein/theme-base is installed but its CSS is not consumed — instead globals.css hand-duplicates all token values. This creates silent drift: when @alawein/theme-base ships a token change, meshal-web won't pick it up until someone manually syncs the 1360-line globals.css. File: src/app/globals.css (entire @theme block).

--- PORTFOLIO PRESENTATION ---
Live site assessment: As a technical recruiter or collaborator, meshal.ai presents a coherent, high-signal narrative. The landing page leads with "AI systems engineer for governed agents, evaluation infrastructure, and scientific compute" with immediate outcome metrics visible. Dark compute-aesthetic is distinctive and on-brand. Individual project pages (e.g., /morphism, /neper, /edfp) provide real technical depth: tech stacks, milestones, live demo links, GitHub audit links. The blog has 9 substantive technical posts. The /cv page with full timeline and the /services page with engagement model are strong professional surfaces.
PhD and background: clearly communicated. src/data/bio.ts:PROFILE contains "Ph.D., EECS, UC Berkeley". The editorial_noir copy.ts:bio.body reads "Ph.D. EECS UC Berkeley. Research in strain-engineered 2D materials and large-scale simulation." The hero headline positions him as AI systems engineer with the Berkeley physics origin story visible in the background section. Uses completed phrasing ("Ph.D." not "pursuing").
Projects showcase: 7 live projects showcased with real descriptions, plus SimCore (external link). Each has a dedicated deep-dive page. Coverage is real — live URLs, PyPI packages, GitHub repos, milestone counts. The portfolio grid at /projects with 8 entries is a genuine technical body of work, not filler.
Contact capability: strong. ContactSection has a working form (POST /api/contact with mailto fallback), direct email (contact@meshal.ai with one-click copy), phone (formatted + tel: href), and a "schedule reliability review" mailto CTA. Contact is reachable through multiple paths.
Key presentation gap: no LinkedIn link in the navigation or landing CTA strip. LinkedIn is present only in CHSH Lab's proofLinks (linkedin.com/in/meshal-alawein) and CONTACT_DETAILS.linkedinUrl exists in contact.ts but does not appear in the site footer or hero section. For a portfolio targeting technical collaboration and employment, the absence of LinkedIn as a top-level surface link is the single most damaging gap.

--- FEATURE GAPS ---
1. LinkedIn not surfaced at navigation/hero level — contact.ts has linkedinUrl but it never renders in MainNavigation, SiteFooter, or HeroSection. Critical for recruiter and collaborator conversion.

2. @alawein/ui components not adopted — the package is declared as a dependency but zero imports exist. The repo maintains parallel local implementations (Button, Card, ServiceCard, etc.). When @alawein/ui ships improvements (accessibility, tokens, animation), meshal-web will not benefit automatically. Gap grows wider as design-system sprints continue.

3. @alawein/theme-base CSS not imported — tokens are hand-duplicated in globals.css (1360 lines). Any upstream token change is invisible to meshal-web until manual sync. The postinstall script (scripts/patch-alawein-tokens.mjs) exists but appears to patch the package rather than consume its CSS output.

4. Stale/unreachable section components — src/components/sections/HeroSection.tsx, ProjectsSection.tsx, AboutSection.tsx, and SkillsSection.tsx exist but are never imported by the active theme path. They should be deleted to reduce maintenance surface.

5. No publications/research section on landing or /about — src/data/publications.ts has MagLogic and LBNL technical report data, but neither the landing page nor /about page surfaces a publications or research output section. For a UC Berkeley EECS PhD positioning in governed AI and scientific compute, publication visibility strengthens credibility significantly.

--- FINDINGS SUMMARY ---
Critical: 0 | High: 1 | Medium: 4 | Low: 1

[HIGH] LinkedIn not surfaced | Navigation/Contact | contact.ts:6 has linkedinUrl, never rendered in nav/footer/hero | Add to SiteFooter and hero social strip | Effort: S

[MEDIUM] @alawein/theme-base CSS not consumed | Architecture | src/app/globals.css:1-1360 duplicates all token values manually; package installed but CSS never imported | Switch globals.css to @import from @alawein/theme-base and remove duplicate @theme block | Effort: L

[MEDIUM] @alawein/ui declared but unused | Design System | package.json:63 — zero imports in src/; parallel local Button/Card/etc. maintained | Evaluate replacing local ui components with @alawein/ui counterparts incrementally | Effort: L

[MEDIUM] Stale unreachable section components | Architecture | src/components/sections/HeroSection.tsx, ProjectsSection.tsx, AboutSection.tsx, SkillsSection.tsx — theme-delegated routes never reach these | Delete or archive stale components; document active theme path in SSOT.md | Effort: S

[MEDIUM] tailwind-merge absent from cn() utility | Component Quality | src/vendor/meshal-site-primitives/cn.ts:1 — no conflict resolution for Tailwind class merging | Add tailwind-merge to package.json and update cn() | Effort: S

[LOW] Publications not surfaced in portfolio | Portfolio Presentation | src/data/publications.ts has real data; no landing/about section renders it | Add a publications/research section to /about or landing | Effort: M
