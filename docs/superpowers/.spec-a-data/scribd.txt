=== REPO: scribd ===
Generated: 2026-04-23

--- TECH STACK ---
Framework: Next.js 16.2.3
TypeScript strict: yes (strict: true in tsconfig.json)
Tailwind version: 4.1.9
Design system packages: @alawein/tokens 0.2.0, @alawein/theme-base 0.3.0, @alawein/ui ^0.1.0
Theme in use: custom brutalist dark theme — overrides token defaults with vibrant oklch color palette (orange/blue/green/gold accents) declared in app/globals.css
State management: none — appropriate for current static content; insufficient once fulfillment is implemented (paymentOption/giftEmail/promoCode are currently discarded server-side — fixing the Critical finding will require state review)
Data fetching: client-side fetch to internal API routes (testimonials); server-side Stripe checkout via Next.js route handlers; no TanStack Query
Live URL: https://scribd.fit

--- SOURCE STRUCTURE ---
scribd/
├── app/                     # App Router runtime
│   ├── api/                 # Route handlers (checkout, payment-methods, payment-plans, testimonials)
│   ├── components/          # DUPLICATE of root components/ (content-preview-modal, handbook-credibility-card)
│   ├── globals.css          # Active CSS entry with design system imports
│   ├── layout.tsx           # Root layout (metadata, analytics, font)
│   └── page.tsx             # Single large storefront page (~1,167 lines)
├── components/              # Shared UI
│   ├── ui/                  # Local UI primitives (button-group, empty, field, item, kbd, sidebar, spinner, toast, toaster)
│   └── theme-provider.tsx   # next-themes wrapper (unused — layout.tsx has no ThemeProvider)
├── hooks/                   # Client hooks (use-mobile, use-scroll-animations, use-toast)
├── lib/                     # analytics.ts, handbook-content.ts, stripe.ts, utils.ts
├── scripts/                 # github-sync-report.mjs
├── styles/                  # globals.css (UNUSED stale light-theme CSS — default shadcn template values)
├── tests/                   # hooks + lib unit tests; visual Playwright snapshots
└── public/                  # Static assets

Routes/pages: app/page.tsx (single storefront page — no sub-routes like /privacy, /terms, /support despite footer links)
Component count: 2 app-level (content-preview-modal, handbook-credibility-card), 9 components/ui/, 1 theme-provider — 12 total
packages/ purpose: no packages/ directory found — the spec-survey entry was incorrect

--- DESIGN SYSTEM ADOPTION ---
@alawein/tokens imported: yes — app/globals.css line 2 (@import '@alawein/tokens')
Theme CSS imported: yes — app/globals.css line 3 (@import '@alawein/theme-base')
@alawein/ui components used: yes — Button from @alawein/ui in app/page.tsx and app/components/content-preview-modal.tsx; transpilePackages set in next.config.mjs
Hard-coded color values: 4 (1 hex in layout.tsx themeColor, 3 raw oklch() values in page.tsx payment option borders as unselected state fallback)
Drift level: Low
Evidence: app/globals.css imports both token packages; globals.css defines a custom brutalist override palette on top of tokens (intentional — not drift); oklch raw values at app/page.tsx:743,754,765 should use var(--border) instead

--- COMPONENT QUALITY ---
Loading states: partial — TestimonialSection has isLoading spinner text; no skeleton for handbook cards; checkout shows "Processing..." on button disable
Error states: partial — testimonial fetch catches and silently shows empty state; checkout catch only logs to console with no user-visible error message (app/page.tsx:360-364)
Empty states: present — TestimonialSection returns "Be the first to share your story" when testimonials empty (app/page.tsx:212-216)
Form patterns: inconsistent — inline <input> elements with raw className strings used directly in page.tsx rather than using @alawein/ui input primitives; testimonial form, referral form, newsletter form, and promo code field all use bare HTML inputs
TypeScript coverage: strict/loose (1 meaningful `any` usage: review: any in testimonials/fetch/route.ts:42; 4 others are Next.js generated types in .next/types/ which are not source)
Key quality issue: app/page.tsx is 1,167 lines and marked "use client" — entire storefront executes as a client component including static copy, handbook data, credibility content, and FAQ items that could be static server components; app/page.tsx:185-251 (TestimonialSection defined inline inside page module)

--- ARCHITECTURE ---
State management decision: currently appropriate for static content rendering; will need revisiting when checkout fulfillment is wired — paymentOption selection and post-purchase entitlement state cannot be handled with UI-local state alone
Data fetching pattern: mixed — checkout via POST to /api/checkout (Stripe-session-redirected — server creates Stripe session only — paymentOption/giftEmail/promoCode sent in POST body are currently ignored), testimonials via client useEffect fetch to /api/testimonials/fetch, handbook content is static module data (lib/handbook-content.ts)
Component organization: dual components/ problem — app/components/ and root components/ contain identical files (content-preview-modal.tsx and handbook-credibility-card.tsx confirmed identical by MD5); @/ alias resolves to root so both exist as dead weight on app/components/
Dead code signals: (1) styles/globals.css is a stale shadcn template (light theme defaults, no custom overrides); it is not imported by any file — app/globals.css is the active entry. (2) app/components/ is a duplicate of root components/ — 2 files × 2 locations. (3) components/theme-provider.tsx exists and is not imported by layout.tsx (ThemeProvider is not in the component tree). (4) checkout/route.ts ignores paymentOption, giftEmail, and promoCode fields that the frontend sends — installment and gift checkout paths are front-end UI only with no actual Stripe Subscription or coupon wiring. (5) No Stripe webhook route exists despite README claiming "Stripe-backed purchase and entitlement flows" — purchase fulfillment and entitlement delivery are not implemented.
Bundle config: images.unoptimized: true is notable for a PDF storefront (no images to optimize); turbopack enabled; transpilePackages: ['@alawein/ui'] correct for ESM package
Key architecture issue: checkout route does not handle paymentOption or promoCode at all — frontend sends them, route silently discards them. Three Stripe payment UX options (full/installment/gift) resolve to identical one-time $190 charge. app/api/checkout/route.ts:5-8

--- PORTFOLIO PRESENTATION ---
Live site assessment: scribd.fit is a fitness handbook storefront selling 4 digital guides ($44–$59 each, $190 bundle). Dark brutalist design, animated hero, handbook cards with accordion details, comparison matrix, testimonials, FAQ, referral form, and newsletter signup. Visually polished and coherent. The design system integration is visible and correctly themed. Payment flow initiates Stripe checkout successfully (though installment/gift paths are not real).
README quality: present at scribd/README.md — concise and accurate. States stack, quick start, validation commands, and layout. Mentions "Supabase-backed auth and access control" and "Stripe-backed purchase and entitlement flows" as shipped features, which overstates reality (auth and entitlement are not implemented — only testimonials use Supabase).
Key presentation gap: README claims entitlement delivery and Supabase-backed auth are implemented. They are not. No auth middleware, no protected routes, no post-purchase content delivery — purchasing via Stripe leads nowhere after success_url redirect. This is the largest credibility gap for a portfolio repo described as a production storefront.

--- FEATURE GAPS ---
1. Stripe webhook + entitlement fulfillment: No /api/webhooks/stripe route exists. Successful checkout never triggers content delivery or purchase recording. This is the core of the product promise.
2. Post-purchase content access: No protected route or download mechanism for PDFs. After Stripe checkout, users land on /?session_id=... with no delivery logic.
3. Installment and gift checkout: Frontend exposes 3-payment installment ($65x3) and gift flows but checkout/route.ts ignores paymentOption and giftEmail entirely — all paths create the same one-time charge.
4. Promo code application: UI input exists with an "Apply" button that calls console.log(); no discount logic, no Stripe coupon/promotion_code integration.
5. Legal/support pages: Footer links to /privacy, /terms, and /support — none of these routes exist. Clicking them returns 404 on the live site.

--- FINDINGS SUMMARY ---
Critical: 1 | High: 2 | Medium: 3 | Low: 3

[CRITICAL] Stripe webhook and entitlement fulfillment not implemented | Architecture | No /api/webhooks/stripe route; no post-purchase delivery; README claims this is shipped | Add webhook handler, purchase recording, and PDF delivery route | XL

[HIGH] Checkout route silently ignores payment options | Commerce | app/api/checkout/route.ts:5-8 — paymentOption/giftEmail/promoCode never read; installment and gift UI are decoration | Implement Stripe Subscriptions for installments, coupon codes for promos, gift flow | L

[HIGH] Duplicate component directories causing dead code confusion | Structure | app/components/ contains identical copies of root components/ (confirmed by MD5); theme-provider.tsx imported nowhere | Delete app/components/, remove unused theme-provider.tsx | S

[MEDIUM] Monolithic client page (1,167 lines "use client") | Performance | app/page.tsx forces entire storefront to client-side render; static content (handbooks array, FAQ, credibility indicators) could be server-rendered | Decompose into server component shell with targeted client islands | M

[MEDIUM] Stale globals.css dead file in styles/ | Maintenance | styles/globals.css is an unmodified shadcn default (light theme); active entry is app/globals.css; stale file creates confusion | Delete styles/globals.css | S

[MEDIUM] Raw oklch values in component inline styles instead of CSS variables | Design system | app/page.tsx:743,754,765 use oklch(0.18 0 0) directly; should be var(--border) | Replace three oklch literals with var(--border) | S

[LOW] README overstates shipped features | Portfolio | Claims Supabase auth and Stripe entitlement delivery are live; neither is implemented | Qualify as "planned" or implement the missing pieces | S

[LOW] No error toast on checkout failure | UX | app/page.tsx:360-364 only console.error on checkout error; no user-visible feedback | Add sonner toast (already in deps) on checkout failure | S

[LOW] Footer links 404 on live site | UX/Portfolio | /privacy, /terms, /support linked in footer but routes do not exist | Create stub pages or remove links | S
