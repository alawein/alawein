[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "librex-qap"
version = "1.0.0"
description = "Librex.QAP - Advanced Quadratic Assignment Problem solver with FFT attractor basin methods"
authors = [{name = "Meshal Alawein", email = "meshal@alaweintechnologies.com"}]
license = {text = "Apache-2.0"}
readme = "README.md"
requires-python = ">=3.9"
keywords = ["qap", "quadratic-assignment", "optimization", "combinatorial", "fft", "attractor-basin", "operations-research"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Operating System :: OS Independent",
]

dependencies = [
    "numpy>=1.21.0",
    "scipy>=1.7.0",
    "matplotlib>=3.5.0",
    "pandas>=1.3.0",
    "scikit-learn>=1.0.0",
    "networkx>=2.6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=3.0.0",
    "pytest-xdist>=2.5.0",
    "black>=22.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
]
quantum = [
    "qiskit>=0.40.0",
    "pennylane>=0.30.0",
]
ml = [
    "torch>=2.0.0",
    "optuna>=3.0.0",
    "ray[tune]>=2.0.0",
]
docs = [
    "sphinx>=5.0.0",
    "sphinx-rtd-theme>=1.2.0",
    "sphinx-autodoc-typehints>=1.23.0",
]
theorem = [
    "z3-solver>=4.12.0",
]

[project.urls]
Homepage = "https://librex.dev/qap"
Documentation = "https://librex.dev/qap/docs"
Repository = "https://github.com/alawein/alawein/tree/main/organizations/alawein-technologies-llc/packages/librex"
"Bug Tracker" = "https://github.com/alawein/alawein/issues"
"QAPLIB Benchmarks" = "https://librex.dev/qap/benchmarks"

[tool.setuptools.packages.find]
where = ["."]
include = ["librex*", "qap*"]

[tool.setuptools.package-data]
librex = ["data/*.json", "benchmarks/*.json", "examples/*.py"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --cov=equilibria --cov-report=html --cov-report=term"

[tool.black]
line-length = 100
target-version = ["py39", "py310", "py311", "py312"]
include = '\\.pyi?$'

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "N", "W", "B", "C4", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
