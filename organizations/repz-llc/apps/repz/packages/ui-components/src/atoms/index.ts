/**
 * Atomic Design - Atoms
 * Basic building blocks of the design system
 * These are the fundamental HTML elements styled with design tokens
 */

export { Button } from './Button';
export type { ButtonProps } from './Button';
