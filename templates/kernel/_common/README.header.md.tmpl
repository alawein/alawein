> **${repo_name}** (`${slug}`) — ${canonical_description}
>
> Managed by the alawein kernel. See
> [`docs/governance/kernel-spec.md`](https://github.com/alawein/alawein/blob/main/docs/governance/kernel-spec.md)
> for what this header covers. Profile: `${profile}`. Kernel version: `${kernel_version}`.
