name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 6 * * 1"

jobs:
  analyze:
    uses: alawein/alawein/.github/workflows/codeql.yml@${workflow_pin_sha}
    with:
      languages: "${codeql_languages}"
